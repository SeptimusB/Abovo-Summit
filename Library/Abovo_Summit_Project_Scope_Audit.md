# Abovo Summit workbook scope audit

## Check Sheet calculation and display coherence — 24 September 2026, 2.98

Ready to test with Jon: functional 80 and related 10/60. A changed visible Check Sheet receives recursive full calculation before override-aware soft-result publication, then targeted mapped-grid refresh. Post-history and warm-activation requests are coalesced against the final revision; ordinary edits while Check Sheet is hidden retain their existing calculation path. Pending override editors, selection and viewport are preserved; rejected/superseded results cannot clear warnings or certify integrity. No workbook formulas, source fills/locks, names, VBA or masters changed.

Both candidate builds pass 124 native assertions on private AGL copies, including eleven full checks each, exact header/value/colour Undo/Redo coherence and no transient TDB Cashflow remainder. Test-only address-space parity fixes a mismatch with Summit's already-LAA 32-bit host; no production memory/platform changes. Normal Debug/Release 2.98 are installed and runtime/quiet-diagnostics verified after Jon closed Debug. See `Library/Check_Sheet_Coherence_2.98_2026-09-24.md` for evidence, hashes, remaining rebind calculation cost and manual Excel/VBA/client acceptance. Tests 1/2/3/5 are Send to Alex Test Response 3; mapped-table zoom/hscroll failure 4 remains separate and unimplemented here.

## Wheel modifiers, contiguous Funding schedules and read-only Model Version — 24 September 2026, 2.97

Ready to test: Ctrl+wheel zoom, Shift+wheel horizontal, ordinary vertical; first-fit contiguous Funding schedule placement or trailing expansion with five spare rows; post-expansion focus; application of existing single-cell RO/calculated XML to text editors. No workbook fill/lock, master or calculation-policy changes. Debug/Release builds and runtime version checks pass, with 81 grid, 9 editor and 39 contiguous-schedule assertions each; existing amount-policy and macro-disabled Excel preservation checks pass. Evidence is recorded in `Library/Production_Changes_2.97_2026-09-24.md`. Working-2.96 remains unchanged. The numbered ledger `Library/Functional_Test_Ledger_2.97_2026-09-24.md` retains IDs 1–83 and adds 84–86, with explicit Jon/Alex/Agreed stages. No new client pass is inferred.

Read-only AGL investigation reproduces the G82 edit, stale Check Sheet after Undo and residual TDB Cashflow check after navigation. It identifies separate deferred-sheet cached results, missing normal warning-state publication and missing watcher-to-visible-grid refresh, not a proven one-sheet dependency-depth limit. A second deferred pass cleared the residual in this specimen but is not implemented as a policy. See `Library/Check_Sheet_Funding_Diagnostic_2026-09-24.md`. Alex's double-click problem remains unconfirmed; isolated event/eligibility diagnostics pass and no speculative behavior change was made.

Follow-up for functional test 80: the independent five-process comparison starts immediately after Undo, before a second navigation. Neither one/two deferred passes nor incremental workbook calculation restores all observed results; recursive full calculation restores the sampled Check Sheet/TDB values to the load baseline. Warning publication and mapped-grid refresh still require separate actions. This rules out treating the earlier sequential extra-pass result as a sufficient repair. See `Library/Check_Sheet_Calc_Matrix_2026-09-24.md` and the proposed, unimplemented `Library/Check_Sheet_Refresh_Design_2026-09-24.md`. Debug/Release 2.97 remain unchanged, reserved for user testing; no calculation policy or production source changed in this follow-up.

## Readable zoom, restored Funding sizing and Rent navigation - 24 September 2026, 2.96

Ready to test: 60% minimum zoom with an 8pt readability floor, batched owner-based repeating-editor sizing, restored-window density rebasing, analyser spacing/clipping, native pixel-anchored Funding zoom, and the unattached-grid constructor repair for the reported Rent navigation crash. Repeated Funding resizing also exposed and repaired stale native header drawing geometry and a blank viewport. Pending edits are retained without posting; workbook fill/lock rules, calculations and original files are unchanged.

Debug/Release builds pass. Final regressions cover 223 Funding assertions in both Release modes, 352 repeating-editor assertions, 63 generic zoom assertions, 31 analyser assertions and 17 Rent-navigation assertions, with detailed per-build evidence in `Library/Grid_Readability_And_Mouse_Zoom_2.96_2026-09-24.md`. A verified frozen `bin/Working-2.96/Abovo-summit.exe` replaces the withdrawn 2.95 working recommendation; 2.95 remains untouched. Jon tests 76, 77, 82 and 83 remain, particularly physical 5K/mixed-DPI testing. Separate Check Sheet heading/TDB reports remain open.

## SpreadsheetGear vendor response - 24 September 2026

The user-provided Tim Andersen support response confirms unsupported grouped-sheet formula-reference fix-ups, custom XML and dynamic arrays. Retain DevExpress production authority; a Gear calculation-only projection remains research, not a customer save/structural engine. Restoring XML parts alone does not restore dynamic-array behaviour. No production engine change, vendor contact, upload or master modification is made. See `Library/SpreadsheetGear_Support_Package_2026-09-24.md` and the linked import/prototype investigation.

## Production zoom, fixed boundaries and links - 24 September 2026, 2.95

Ready to test: batched repeating-editor zoom, stable fonts/row sizes, analyser context zoom/reset and bounded description widths, compact unclipped totals, clear three-pixel fixed-region boundaries, restored Facility filtering, Undo/Redo calculation for otherwise unregistered source sheets, and single-click Check Sheet Summit destinations. Fill-based locking and original workbooks are unchanged. Debug/Release builds and targeted native regressions pass; Jon functional tests 76-81 and client/Excel-VBA acceptance remain. The separate Check Sheet public-heading/TDB reports and engine research are not declared resolved. Evidence and boundaries: `Library/Grid_Zoom_And_Links_2.95_2026-09-24.md`.

## Grid presentation and Funding scheduling — 24 September 2026, 2.94

Ready to test: common vertical/Ctrl-horizontal/Shift-zoom behaviour across native table, vertical and tree grids; context zoom/reset and local saved preferences; Funding sticky loan names with identity hints; funder/facility filters with an orange warning/clear action; native drag selection and copy with loan/date titles. Filters preserve physical worksheet-column mapping for edit/paste/navigation/scheduling. Persistent keys use XML source names, not regenerated control/dataset counters. Scheduler restores occurrence count and contextual section defaults, retaining valid dates/figures while softly reporting skipped locked/occupied/formula targets. Fill-based locking is unchanged. Stock Description minimum is halved.

Debug/Release builds pass. Four final Release disposable-copy runs pass 58 UI, 85 scheduler/round-trip, 21 amount-policy and 17 fill-protection assertions. Original masters are unchanged. Jon tests 68–75, including full reopen/mixed-DPI and Excel/VBA acceptance, remain; Check Sheet heading/TDB issues remain open. Full evidence and boundaries: `Library/Grid_Presentation_Funding_2.94_2026-09-24.md`.

## Client UI and simpler Funding schedules — 24 September 2026, 2.93

Ready to test: same-day recurring dates with inclusive end date and monthly/quarterly/semi-annual/annual intervals, one-column preview, multi-digit fixed figures, loan-targeted right-click schedule action, corrected Facility list, dropdown/Shift+Down handling, bounded company editor and pivoted Stock minimum width. Actual wheel tests found and repaired the embedded-form active-window guard; normal Funding wheel is vertical, Ctrl horizontal, with modal exclusions retained. Debug/Release and private-copy regressions pass; fill-based locking and financial calculations are unchanged. Review v05 retains Jon/Alex colour gates and new tests 61–67. Check Sheet public-header refresh and the reported TDB remainder stay open; zoom investigated, not implemented. See `Library/Client_UI_And_Scheduler_2.93_2026-09-24.md`.

## Full calculation for Check Sheet trial — 23 September 2026, 2.92

Ready to test: the user-requested Check Sheet watcher now runs CalculateFull with deferred-sheet suppression temporarily lifted, then reads the existing override-aware soft balance result. No dependency rebuild or integrity-policy change. A controlled stale-result regression detects and clears a cross-sheet imbalance and preserves engine/mode, dirty/rebuild flags and unsaved-warning persistence boundaries. Debug/Release updated; Jon functional test 60 remains. See `Library/Check_Sheet_Full_Trial_2.92_2026-09-23.md`.

## Funding schedule figures and dialog scrolling — 23 September 2026, 2.91

Ready to test: taller scheduler, DIT wheel routing constrained to its own active/enabled interface, clearer target tints/markers without changing locks or Excel fills, and optional fixed figures for the selected facility. Date-dependent admission checks execute inside ChangeManager rollback; dates and figures share one Undo/Redo. Decimal, percentage, rejection, native UI, fill-lock and macro-disabled Excel-copy checks pass. Normal Debug/Release updated. Jon's stable functional items 57–59 and client/financial acceptance remain. See `Library/Funding_Schedule_Figures_2.91_2026-09-23.md`.

## Funding schedule groups — 23 September 2026, 2.90

Ready to test: Settings OK replaces Apply; automatic schedule previews; facility selection and record-header plus; multi-section date posting with one grouped Undo; Summit-only bounded colour tints. Separate versioned workbook custom XML and hidden cell anchors preserve associations through save/recovery, Excel round-trip and structural movement without modifying Excel fills or adding worksheets. Full Debug scheduler, Release metadata/multi-expansion, blank fill-rule and macro-disabled Excel round-trip checks pass. Normal Debug/Release updated; Jon functional items 54–56 and Alex/financial VBA acceptance remain. Details: `Library/Funding_Schedule_Groups_2.90_2026-09-23.md`. No commit/push requested; unrelated edits retained.

Audit date: 22 August 2026; authority updates: 4 September and 11 September 2026

## Recovery discovery delete option — 23 September 2026

**2.89** adds a confirmed, identity-checked Delete recovery choice alongside Open recovery / Open original / Cancel. See [stable test 53 and evidence](Recovery_Delete_2.89_2026-09-23.md). Release and isolated Debug each pass 31 native/synthetic-file assertions; normal Debug installation awaits closure of the running session. Existing client recoveries, originals and workbook content are unchanged. Ready to test with Jon.

## Rent Weeks and Funding schedules — 23 September 2026

**2.88** adds positive-decimal Rent Weeks validation and the approved native Funding date scheduler. See [Rent Weeks / stable test 51](Rent_Weeks_2.88_2026-09-23.md) and [scheduler / stable test 52](Funding_Schedule_2.88_2026-09-23.md). Dates use ChangeManager, automatic capacity uses the established row-insertion service, and existing XLSB fill-based locks remain unchanged. Thirteen fixed-row Funding bindings now use existing named-range anchors to follow inserted rows. Debug/Release builds, disposable Demo/Blank native tests, GOV.UK calendar downloads and a macros-disabled Excel round-trip pass; original workbooks and all 337 VBA module sources remain unchanged. Ready to test with Jon; older/bespoke layouts and financial/VBA/client acceptance remain separate.

## Pattern-based editability indication - 23 September 2026

**2.87** restores the shared DIT unavailable-input cue removed by the 2.84 readability change, retaining readable text and unchanged XLSB fill-pattern locks. Debug/Release native Stock editor, paste-eligibility, Undo/Redo/clear and drawing regressions each pass 17 assertions on disposable Blank copies. See [repair evidence and functional test 50](Pattern_Editability_Cue_2.87_2026-09-23.md). Ready to test with Jon; no broader conditional-format parity or client acceptance is claimed.

## Explicit missing snapshot creation - 23 September 2026

**2.86** implements the approved on-demand snapshot-sheet provisioning. Only Create Snapshot adds missing TDB Snapshot / TDB Comparison sheets and positions the dedicated pair immediately after Transactional DB; ordinary load/probes remain non-mutating. Demo, Blank and older Stori private-copy regressions pass, including save/reopen; Demo/Stori Excel round-trips preserve frozen values/order/names and original VBA module sources. Source workbooks remain unchanged. See [snapshot validation](Snapshot_Provisioning_2.86_2026-09-23.md) for exact coverage and remaining Jon/client acceptance. [Separate engine research](Spreadsheet_Engine_Trial_Assessment_2026-09-23.md) recommends an isolated Aspose.Cells experiment, not a production replacement; no candidate is installed or benchmarked.

## Client review first batch — 23 September 2026

**2.83** [review checkpoint](Client_Review_Batch_2.83_2026-09-23.md) adds committed company-name metadata refresh across open headings and FileInstance, consistent ordinal-year labels, and the requested DIT Return/History artwork and ordering. Debug and Release build successfully and each passes 31 native private-copy assertions. The normal Debug executable is updated after the user closed the session. Diagnostics stay disabled. [Review v02](Client_Review_2.83_v02.docx) records Alex's pending confirmations without treating them as acceptance; source workbooks, original report and v01 are unchanged. Other outstanding client issues are not closed by this batch.

## Quiet client test checkpoint — 23 September 2026

**2.82** [delivery record](Client_Test_Delivery_2.82_2026-09-23.md) includes the approved 2.80/2.81 repairs, temporarily disables Summit diagnostic tracing and passive benchmarks, and retains functional timers, progress, history and integrity/recovery notices. One build property restores diagnostics. Both configurations build and quiet-gate/native regressions pass. The numbered client report [review v01](Client_Review_2.82_v01.docx) marks believed fixes orange and adds questions; unresolved conditional-format, older fee mapping and layout items remain open. Original workbooks and the source report are unchanged. Client will build and arrange acceptance; no push/installer is implied.

## Remaining client input review — 23 September 2026

Approved follow-up: **2.81** [typed-input/source-mapping checkpoint](Client_Input_Repairs_2026-09-23.md) repairs monetary definitions/display, invented percentage limits, Covenant year/RO bindings, current-template one-off fee BP-year typing and empty Survey dates. Shared native-grid/paste numeric validation retains workbook/XML bounds without trial writes. Debug/Release builds, native Demo/Blank/applicable AGL tests, save/reopen and independent Excel stored-value checks pass; original workbooks and VBA module sources are preserved. AGL's older Other Fees row coordinates are a newly confirmed outstanding mapping problem, not covered by the current-template fee fix. Conditional formatting/dependent editors and unresolved layout questions remain the next batch; financial and client acceptance remain separate.

The 2.80 [input/presentation diagnostic checkpoint](Client_Input_Review_2026-09-23.md) confirms remaining decimal storage/display, percentage bounds, Covenant mapping/read-only, Funding fee prerequisite/year-type, blank-date and conditional-format issues. It separates native paste/amount regression passes from unanswered layout/ghost-date questions and physical client acceptance. This is review only; source workbooks and production code are unchanged by this checkpoint.

## 2.66 checkpoint priorities and minimum specification — 22 September 2026

The user sets 32 GB RAM minimum, Core i5 acceptable/Core i7 or better preferred, Windows 10 or later. Exact CPU generation and supported OS/Excel build matrix are not yet specified. The earlier suggested 8/16 GB VM profiles are superseded. Performance is explicitly prioritised over continuously checked accuracy, subject to occasional integrity checks. The follow-up is to define that checking cadence while preserving pending-result signalling, visible integrity failures, transactional safety and workbook/VBA round-tripping; this documentation checkpoint does not silently change the current runtime policy. See `Library/Outstanding_Tasks_2026-09-22.md` for the consolidated work/acceptance list and commit scope.

## Recovery backups and Excel/format investigation — 22 September 2026

Summit 2.66 adds opt-in idle-scheduled `_recovery.xlsm` backups (10 minutes by default, configurable 1–120) without changing normal XLSB Save, the original bytes, live dirty state or Undo. Reopening a newer owned recovery offers read-only prior-session history, recalculates pending results and requires guided XLSB Save As. History uses standard custom XML, not extra worksheets. Recovery serialization is on the UI thread and has progress notification; it is not concurrent background workbook access.

The user explicitly approved adding noninterfering workbook data and missing snapshot worksheets when needed. This approval does not permit arbitrary load-time schema changes or waive Excel/VBA, collision/protection and 3-D sheet-order safeguards. No extra sheets were needed or added for this recovery feature.

Recovery shows a named-file saving notice, brief completion/failure, and System Messages. Due saves defer while all Summit windows are minimised, rather than running silently. The shared progress form now wraps/resizes within the monitor working area and scrolls oversized messages. Debug/Release builds and native recovery/progress tests pass, with rendered normal/150%/200% font checks. Status: Ready to test; physical DPI/client acceptance remains separate.

A direct XLSB-to-XLSM exporter omission was reproduced and isolated to missing dynamic-array metadata. The narrowly guarded recovery bridge supports the identical 117-byte single-XLDAPR profile verified in Blank, Demo and frozen AGL; it adds standard metadata without rewriting worksheet formulas. Unknown metadata fails safely. Native preservation, history/Undo/state/failure and Excel no-repair/VBA-source/custom-XML checks are documented in `Library/Recovery_Backup_Trial_2026-09-22.md`; manual client/VBA/financial acceptance remains required.

Excel reread is not a blanket formula-text equality result: saving recovery XLSM removes six verified function-name placeholders; saving recovered normal XLSB adds implicit-intersection SINGLE wrappers to 1,140 Development Expenditure formulas. Independent full native recalculation agrees across 44,906 relevant cells before/after the latter Excel save. All 335 VBA module source hashes and original custom XML/history are preserved. This does not replace financial/VBA acceptance; exact cases and evidence are recorded in the recovery report.

Three-run format measurements favour retaining XLSB for normal use: native median open 10.754s versus XLSM 29.813s, while save is 13.285s versus 7.940s excluding preparation. Desktop Excel is faster with XLSB for both operations on this sample. Optional Excel structural offload remains a proposal requiring controlled VBA entry-point/security/rollback/history design, not an implemented production shortcut. See `Library/Excel_Offload_and_Workbook_Format_Investigation_2026-09-22.md` for raw-result locations, compatibility limits and isolated structural probes.

Client-test release reminder: revisit the deferred persistent recent-files HTML launcher; no implementation is included in 2.66.

## First-save distinction and DIT toolbar - 22 September 2026

Test release 2.65 separates the initial unverified output graph from an actual mutation requiring save-time rebuild. A first value-only save can defer calculation while retaining compatibility preflight and the persisted pending-results/load-reader gate. Formula repair, structural/unknown changes and failure/recovery guards remain conservative. The AGL first-edit trace confirms zero save-time rebuild and zero engine restoration; formula scanning and native XLSB writing remain. DIT buttons now follow the user's exact grouped order, verified through native hit positions rather than collection order alone. Shared icon-only panel toggles use Maximise working area/Restore sidebars wording. Debug/Release, synthetic state, populated AGL save/reopen and native DIT save/toolbar tests pass. Source workbooks remain untouched; client/Excel-VBA/financial acceptance remains manual. See `Library/First_Save_and_DIT_Toolbar_Trial_2026-09-22.md`.

The 2.65 trial also disables Save for clean models in DIT and File Instance while leaving Save As available. Pending unposted DIT edits temporarily enable Save so the normal first-click editor-commit path is retained; successful saves clear the buttons and subsequent dirty-state transitions update both windows. The observer does not calculate or modify the workbook. See the 2.65 report above for native UI/lifecycle evidence.

## Fast input-save policy - 22 September 2026

Test release 2.64 implements the approved deferral of whole-model calculation for verified value-only saves. ChangeManager, worksheet calculation and XML `IsCalculated` grid updates are retained and tested before Save. A standard XLSB custom document property records pending full results; Summit's full-model load and output-reader gates calculate before consuming them. Summaries explicitly identify pending results, and retained FFR/Stress Test activation is covered. Initial unverified, structural/formula, failure/cancellation and close/recovery safeguards remain conservative. No metadata sheets or original-workbook edits are introduced.

Debug/Release builds, synthetic lifecycle/compatibility, x86 Demo/AGL edit-save-reopen and standalone-history regressions pass. Calculated reader/reopen results match 11,900 output cells per model. A macro-disabled Excel copy preserves the pending marker, 12,483 selected cells and all 337 Demo VBA source hashes; this is preservation evidence, not independent financial calculation. Value-save observations are about 9 seconds Demo and 14-15 seconds AGL, now predominantly native serialization; the later output/reopen calculation still costs time. Status: Ready to test. Trusted Excel/VBA, financial and client interactive acceptance remain manual. Exact policy, source identities, timings, and test limits: `Library/Fast_Input_Save_Trial_2026-09-22.md`.

## Separate formula-preflight state - 22 September 2026

Test release 2.63 adds per-model formula/structure verification state distinct from dirty/calculation state. After the first successful checked save of an open model, ordinary typed value edits and value Undo/Redo skip the full formula scan. Formula/name/structural/unknown changes and failed/cancelled saves invalidate it; a calculation alone does not certify export compatibility. This supersedes 2.62's every-save formula preflight, not the XLSB compatibility guard itself. The full result calculation is retained: a tested incremental candidate left two Check Sheet cells stale in the two-active-interface case, with no useful timing gain.

Debug/Release builds and final x86 Demo/AGL, state and compatibility tests pass. Rent/stock caches agree with separate full rebuilds, and saved-file reopen preserves 11,900 checked output cells per model. Demo's macro-disabled Excel-copy/native reopen preserves 12,483 selected cells and all 337 VBA source hashes. The populated stock-edit save records 0 ms formula preflight and 23.641 seconds total; native save and engine-restoration costs remain and are now separately traced. Original hashes are unchanged. Status: Ready to test; client UI, financial and trusted Excel/VBA acceptance remain separate. Full policy, conservative first-save behavior, rejected experiment and fixture limitations: `Library/Save_Formula_Preflight_Cache_Trial_2026-09-22.md`.

## Save preparation and clean-close state - 22 September 2026

Test release 2.62 tracks rebuild and calculated-revision state per open model. Structural/formula/unknown changes require a full rebuild; ordinary edits receive a full result calculation without rebuilding; unchanged saves reuse preparation. Clean healthy close skips calculation/check inspection, while known validation failures and recovery protections remain. Save As refreshes the native FileInstance HTML and open sidebars after confirming the new path. Delayed load/save notifications no longer dirty a clean model. This supersedes the unconditional rebuild/close policy described for 2.58 and 7.30 below, without removing XLSB formula compatibility preflight.

Debug/Release, private Blank/Demo and populated x86 Debug AGL tests pass, including real typed edits, Undo/Redo, browser DOM, failure/cancellation and independent native full-rebuild cache equivalence. Macro-disabled Excel copy/native reopen preserves 12,483 selected cells and all 337 Demo VBA module hashes. Full macro-disabled Excel recalculation is not a passed oracle: the same NAME errors occur on the untouched Demo baseline. Source hashes remain unchanged; interactive Excel/VBA/client financial acceptance remains outstanding. Status: Ready to test. See `Library/Save_Rebuild_State_Trial_2026-09-22.md` for state rules, precise evidence and limits.

## Authorised client Funding recovery - 22 September 2026

A separate copy of the specifically identified damaged 26.0001 client workbook has been repaired: 15,124 proved Funding formula corrections, six ordinary-loan name extensions and two native TDB mirror expansions, plus the separately approved master-verified dashboard B41 restoration. All 714,535 original formula cells have zero unexplained differences and all 87,848 original constants are preserved. The 43 Check Sheet statuses pass; Excel recalculated outputs agree with Summit and all 339 original VBA module source hashes survive the round trip. Original client/master hashes remain unchanged. This is a hash-locked offline recovery, not a load-time migration or new app release; version remains 2.61. Status: Ready to test. Client/accountant and interactive VBA acceptance remain separate. Exact source/output identities, evidence and limitations: `Library/Client_Funding_Repair_2026-09-22.md`.

## Development single-capture optimisation - 22 September 2026

Test release 2.61 uses one full 3-D capture for Identified Development's adjacent split insertion, advancing the operation-local inventory and inspecting newly copied columns before the second batch. A final tracked-reference verification is retained. Funding, Multi-year, deletion, TDB sizing, save checks and original workbooks are unchanged. The second pass drops from 7.679 seconds to 0.083 seconds in the AGL +10 trial; total is 98.330 seconds. Debug/Release builds, full saved-formula comparison, native add/delete, Excel round-trip, VBA hash, failure-recovery and Automatic-mode checks pass. Status: Ready to test; financial and interactive VBA/client acceptance remain separate. Detailed scope, timing limits and validation status: `Library/Development_Single_Capture_Trial_2026-09-22.md`.

## TDB engine-scope optimisation - 22 September 2026

Test release 2.60 shares the guarded Recursive calculation scope across source insertion and existing TDB synchronisation for Identified and Multi-year Development only. It restores the caller's engine/mode before interface notifications and avoids one redundant chain rebuild. The paired fresh x86 AGL Identified +10 run improved from 109.662 to 103.290 seconds. Funding was tested but left on its existing sequencing because repeat timing did not demonstrate a gain. Whole-row/union TDB insertion alternatives and source-history suppression were also not adopted. TDB row-shift algorithms, snapshots, normal edits, workbook schema, VBA and save guards are unchanged.

Both final builds pass. All 1,147,061 checked AGL Development formula cells and saved outputs agree; macro-disabled Excel round-trip checks preserve 12,483 selected cells and all 335 VBA module source hashes. Final add/delete, leading/sentinel/revolver boundaries, native Analyser/snapshot deferral and refresh, injected sync failure and automatic-mode restoration pass on private workbooks. Originals are unchanged. The warm snapshot-present structural case remains expensive; no improvement is claimed for it. Status: Ready to test; financial/interactive VBA and client acceptance remain separate. See `Library/TDB_Engine_Scope_Trial_2026-09-22.md` for complete timings, rejected trials, fixture limitations and reproduction.

## Insertion optimisation follow-up - 22 September 2026

Test release 2.59 uses operation-local 3-D span metadata and a faster conservative candidate screen, retaining the public native formula parser and all reference safeguards. Funding and both Development families use the existing temporary Recursive mutation option with entry-state restoration. Paired fresh-process x86 AGL +10 tests improve Funding from 122.113 to 102.140 seconds and Identified Development from 143.484 to 110.840 seconds, excluding load/save/UI. Full checked formula/name/input/output comparisons agree; final add/delete safeguards, macro-disabled Excel output/round-trip checks and all 335 AGL VBA source hashes pass. TDB shifting itself is unchanged and remains a major cost. No originals, workbook schema, VBA, insertion sequence, normal edit calculation or 2.58 save safeguards changed. Debug and Release are published; client UI/financial/VBA acceptance remains separate. Evidence and timing boundaries: `Library/Insertion_Optimisation_Trial_2026-09-22.md`.

## XLSB save compatibility follow-up — 22 September 2026

Test release 2.58 safeguards FileManager model saves against the demonstrated DevExpress 25.2.4 long-argument XLSB formula loss. The public syntax-tree rewrite nests only verified CONCATENATE calls (31–60 arguments); other unsupported long calls block saving with a location. Pre-save Recursive full rebuild prevents the AGL Funding stale-cache discrepancy without adding a full rebuild to each edit. Cancelled/failed saves retain dirty state and restore formula/calculation settings. Private Blank/Demo/AGL, 32-bit, native and macro-disabled Excel checks cover the repaired paths; 335 AGL VBA source hashes survive Summit–Excel–Summit. Supplied originals and repository masters are untouched. Previously erased formulas are not automatically recovered. Accountant/Excel-VBA acceptance and insertion-speed optimisation remain pending. Full evidence and limits: `Library/XLSB_Save_Compatibility_Trial_2026-09-22.md`.

## Current authority

### 22 September 2026 — worksheet protection performance

Trial 2.57 uses standard Excel-compatible worksheet password verification when the shared WSSecurity service re-protects sheets, as authorised after the Funding debugger stack showed strong password hashing. Passwords, supplied permissions and locked cells are retained; workbook-wide verifier settings are restored in Finally. There is no load-time migration, encryption change or source-master rewrite. Funding/Development traces now separate unlock/re-lock costs. `Library/Worksheet_Protection_Performance_2026-09-22.md` records native/Excel verification, structural checks and the remaining column-shift/TDB costs; do not interpret this as a guarantee against long-operation debugger warnings.

### 22 September 2026 — approved structural repair trial

Trial 2.55 implements the approved Development copy-order, seven physical row axes, new-row input clearing and linked Service/Specific/Other Income, Joint Venture and Intercompany Funding contracts. It preserves array formulas, companion named ranges, existing TDB synchronisation and entry protection/calculation state. Disposable native and independent Excel comparisons are recorded in `Library/Structural_Repair_Trial_2026-09-22.md`; that report controls validation status and gaps. Masters/client originals are unchanged. OFA/Repairs anchor parity and remaining family-specific deletion limits are not declared resolved. Financial and interactive Excel/VBA acceptance remain separate.

### 21 September 2026 — DIT Save, Funding repair and full VBA structural audit

Test release 2.54 adds DIT Save / Save As using existing model services and the separately approved Funding insert/delete repair. Funding now stages all 32 linked-sheet insertions before template copying, matches the VBA anchor, handles 3-D references through the formula parser, and protects the first ten ordinary-loan columns plus revolvers. Blank and Demo each matched 104,014 linked-sheet formula cells against an independent Excel grouped reference and after Excel save-copy/reopen; all 337 VBA module hashes survived. Original masters and the already damaged client workbook are unchanged. See `Library/Funding_Insert_and_DIT_Save_2026-09-21.md` for exact evidence and remaining financial, VBA-interactive and client-DPI acceptance.

The subsequent full master VBA review inventoried 337 modules / 287 procedures / 18,470 lines and reviewed all executable procedure text, plus 46 native rules and 58 XML expansion declarations. It reproduced Development copy-order differences, Specific Income wrong-axis insertion, Cash Journal input duplication and Service Charge input/workings range omissions. Other coupled worksheet-event paths and OFA/Repairs anchor differences need further repair/comparison. These are reported, not silently fixed or declared safe: `Library/Master_VBA_and_DIT_Structural_Audit_2026-09-21.md` and `Library/Master_VBA_Inventory_2026-09-21.json`. No VBA macros executed; no raw VBA or credentials persisted.

### 21 September 2026 — presentation, Journals and funding-payment dates

Test release 2.53 repairs the reproduced Journal Add Lines defect by explicitly resizing IR_Journals (excluding the Rep_Jour_01 sentinel) before synchronising both debit/credit mirrors. Previously inconsistent files fail before mutation rather than being silently repaired. Funding's First Interest Payment Month now retains Excel date values and the exact FundMonthDesc validation list instead of month-name text. Presentation changes include the FileInstance summary, Check Sheet gridlines, analyser Balance Sheet styling and sidebar defaults/alignment. Native tests passed; expanded Demo passed Excel read-only/copy/reopen with Journal Check Sheet OK and all 337 VBA module hashes unchanged. Originals were not edited; physical-DPI, accountant and interactive-VBA acceptance remain separate. See `Library/Presentation_Journals_Funding_Trial_2026-09-21.md` for scope and evidence.

From 11 September 2026, `Library/Blank BP v26_0001.xlsb` is the authoritative unpopulated master and `Library/Demo BP v26_0001.xlsb` is the authoritative pre-populated master and Debug auto-load workbook. Both are exact copies of the user-approved `C:\Sandbox` sources:

- Blank size: 11,643,967 bytes
- Blank SHA-256: `0B4C06800FE998E8733A5D8EB9CDEFA04F517750275BB0CFDD532928900F5B79`
- Demo size: 12,617,838 bytes
- Demo SHA-256: `1ED79726D8D1129C699242C9E8B3E988AC920EA7108D3C0B105BF9B0BC8A5E9C`
- Worksheets: 285
- Defined names: 1,761, with no broken or external definitions
- Generated formulas: 678,983 in Demo; 678,969 in Blank
- Blank difference: fourteen unlocked dummy/input formulas deliberately cleared from `Development BP Assumptions!S86:U86` and `Stock Condition Inputs!O9:O19`
- Formula Generation VBA: disabled by fourteen paired `FormGenRemoval 11/9/26` markers, five guarded core procedures and inactive startup/worksheet-change entry points
- `Transactional_Records`: `Transactional DB!A6:BV1599` (1,594 rows by 74 columns)
- Close validation range: `Outputs_CheckSheet = Check Sheet!A9:H63`
- Embedded VBA project: present

`Library/TestFileClean.xlsb` is retained as the preceding contract baseline. Its last authoritative identity was:

- Size: 11,813,448 bytes
- SHA-256: `D2CA9A14B432C6914612917129F20446DDD33A9BD7740AFAC704A5C357790C17`
- Worksheets: 283
- Defined names: 1,758
- `Transactional_Records`: `Transactional DB!A6:BV1599` (1,594 rows by 74 columns)
- Period fields: 40, from `2025/26` to `2064/65`
- Close validation range: `Outputs_CheckSheet = Check Sheet!A9:H63`
- Embedded VBA project: present
- ChatGPT-added `__Abovo_*` worksheets/names: none

The source workbook was inspected read-only through Excel automation with macros and events disabled and was never saved. The repository copy hash was verified against the source immediately after copying. The user owns external backup management; Summit does not create or retain an internal master backup during this replacement.

## Transactional DB contract

The workbook's formula-backed `Transactional DB` sheet and its workbook-defined names remain authoritative. `TransactionalDBSynchroniser` implements the same named-range sizing rules as the workbook's `Summit_Compatibility` VBA layer; it does not create hidden template, metadata, or materialisation sheets.

The twelve canonical source rules and all 89 `TransCopy_*` targets were checked read-only. Every target exists on `Transactional DB` and has the expected row count derived from its source named range. Development Identified therefore uses the original `TransCopy_DevptSingle_A:N` ranges on `Transactional DB`, not auxiliary sheets.

Structural commands now return a failed `AbovoTransaction` if post-change Transactional DB synchronisation fails. Interface dependency invalidation still runs so an already-changed workbook cannot leave visible controls appearing current.

### Development mirror capacity decision - 4 September 2026

Following client review, the reserved-capacity prototype was withdrawn. The authoritative workbook contains no `_Capacity` or `_Continuation` names. `TransactionalDBSynchroniser` and the workbook's existing Excel/VBA implementation both retain the established structural-shift method for expanding or contracting Transactional DB mirror ranges.

The separate `Z:\Sandbox\TestFileClean - Capacity Aware VBA Candidate.xlsb` file is retained only as a proof of concept. It is not a production contract or repository master and did not complete full runtime and round-trip validation before withdrawal.

## Analysis interfaces

Both `BPIncomeExpenditureAnalyser` (V1) and `BPIncomeExpenditureAnalyserV2` bind the canonical `Transactional_Records` range. The Sample range provides the original 74 datasource columns, including 40 periods. V1 adds its `ItemDesc` column as the 75th unbound grid column; it is not an XLSB field. V2 discovers period columns and required grouping fields dynamically and uses the same unbound description column.

The model resource registry continues to enforce exclusive ownership of the live `Transactional_Records` RangeDataSource, allowing V1 and V2 to disconnect and reconnect safely.

## Removed schema-10 implementation

`WorkbookMigrationManager` and `TransactionDBMaterialiser` have been removed from the project and model lifecycle. Full-model open no longer mutates workbook schema. The Development-specific materialiser branch and its `__Abovo_DevMat_Test` diagnostics have also been removed from `TransactionalDBSynchroniser`.

The retired schema-10 audit, index, and workbook analysis are retained with `.Schema10` filenames for historical evidence; they are not current product contracts.

## Validation boundary

### Model shutdown ordering

Version 7.28 keeps `ExcelModels(ModelID)` and its workbook registered until registered analyser resources and all group/data interfaces have detached. DataInterfaceTemplate now removes unbound callbacks before clearing grid columns or bindings, disposes its data sources and controls idempotently, and returns no value from a late callback once the model is marked as closing. Group interfaces are disposed directly during final model shutdown rather than passing through their cancel-and-hide user-close handler.

### Merge-down Add Lines metadata

Version 7.29 preserves `ISEDatasource.RowExpandByNR` when constructing `MergeDownAndPivot` datasets. This restores the workbook-owned Add Lines targets for Development Details (`HouseTypeInID`) and Development Dets. Multi-Year (`HouseTypeInMY`) without changing `Structure.xml` or either named range.

### Close calculation and Check Sheet validation

Version 7.30 performs a recursive full-rebuild calculation before any model close, temporarily includes the Transactional DB in calculation, and then examines every populated status in the workbook-defined `Outputs_CheckSheet` range. Blank section headings are ignored; `OK` is the only passing populated status. Calculation errors, missing/malformed validation ranges, formula errors and workbook check failures all prevent normal close. The user may cancel and return to the model, or continue only by saving a macro-preserving XLSB copy to a different path; the validation path cannot overwrite the original model.

### Explicit structural Add Lines rules

Version 7.31 adds an explicit `StructureRuleID` contract from `Structure.xml` through `ISEDatasource`, `DataCellRange` and `ActionToken`. Multi-range Assumptions grids now declare their semantic workbook rule instead of depending on the displayed named range being recognised as an implicit alias. Declared rules fail closed if the rule manager is unavailable or the ID is unknown, so Summit cannot silently fall back to resizing only one named range. Legacy single-range grids retain named-range inference and the original generic expansion path.

Version 7.34 restores the required domain-event lifecycle for declared structural grids. `StructureAddCommand` and `StructureDeleteCommand` flow with the rule ID into the DIT action; the requested count is then dispatched through `EventCoordinator`, `BPGridEvent` and the matching `SpecificRowColumnEvents` sub before `WorkbookStructureRuleManager` performs the multi-range mutation. Contraction from the footer uses the matching delete event with an explicit delete-last count. Missing or unknown commands fail closed. A live disposable-workbook trace confirmed that adding five Capital Grant columns follows `RunAction` -> `ProcessAddCapGrantRecords` -> `InsertCapGrantColumns` with a requested count of five.

Version 7.35 inserts Capital Grant records immediately before the workbook's hidden/template grant column. This keeps the insertion inside the workbook-owned Capital Grant ranges, so the assumptions names, all three Capital Grant Workings multi-area names, and existing Transactional DB `INDEX` source ranges expand together before `TransactionalDBSynchroniser` adds mirror rows. A read-only comparison of `TestFileClean.xlsb` and the failed validation copy isolated the former stale `$D$109:$H$148` reference; an unsaved insertion simulation expanded it to `$D$109:$M$148` and produced valid mirror formulas for records 1-10. Grid action extenders now force their DevExpress view layout before the selected section's best-size pass, ensuring the footer `+ Add lines` surface is included on initial display rather than only after a tab round-trip.

Version 7.36 applies the same workbook-owned template-boundary treatment to every `SpecificRowColumnEvents` rule where the authoritative XLSB requires it. Capital Expenditure and Housing Components now insert immediately before their trailing structural/template columns, so linked calculation ranges and Transactional DB source formulas expand with the user records; an unsaved read-only-workbook simulation expanded the relevant source spans from `OFA Additions!E:I` to `E:J` and `Component Totals!D:H` to `D:I`. Repairs already inserted within its dependent ranges, so only its assumptions-grid width source was corrected: formulas and formats still come from the zero-width template while new visible categories inherit the preceding visible column width. OFA, Funding, both Development rules, Journals and Stock Conversion were verified against their named-range geometry and deliberately left unchanged because their existing insertion points already preserve their formula dependencies or their append semantics.

### Funding Assumptions V2

Version 7.37 adds a workbook-backed `Funding Assumptions V2` child alongside the original interface. Its four tabs cover facilities and loans, variable and cash rates, other fees, and investments through the authoritative `Funding Assumptions` names/ranges. Funding Details now includes `Rep_Fund_08` (`Funding Assumptions!E81:R81`) as the final Opening Balance line and remains fixed at the top of the internally scrolling VGrid. Commitment Fees retains its calculation-method selector and now exposes the same in-place date-header editor and named-range row action as the earlier event sections. Native date-valued DIT fields use typed DevExpress date editors and continue to write through the established workbook change path.

Version 7.38 removes the historical assumption that a child structure's XML `CSID` must equal its zero-based list position. `GroupStructure.ResolveChildStructure` first preserves the fast positional path where ID and position agree, then resolves an explicit XML ID, with a final positional fallback only for legacy blank or non-numeric IDs. Presentation, datasource and group-caption lookups all use the resolver. This allows Funding Assumptions V2 (`CSID 138`) to coexist between Funding and Interco Funding without renumbering or redirecting any established Assumptions child.

### Workbook-backed grid clipboard support

Version 7.39 completes the DIT vertical-grid clipboard contract and extends it to the specialised FFR and Stress Test views. DIT vertical grids now use DevExpress cell multiselect and header-free native copy while retaining the existing transposed paste mapping and `ModelChangeManager` write path. FFR Workings, FFR Inputs Adjustment Statement and the editable Key Definitions Column C accept rectangular paste from the focused VGrid cell; locked/non-solid workbook cells and read-only statement views remain protected. The editable Stress Test first-tab, planner and target grids use their existing workbook-address resolvers for paste, while captured/output grids remain copy-only. Clipboard values are converted using the target workbook/data format and every accepted cell is processed through `ModelChangeManager`; no direct workbook assignment was introduced.

Version 7.40 unifies DIT clipboard entry points. The existing WindowsUI Copy and Paste buttons now act on the last focused DIT GridControl or VGridControl and call the same copy/custom-paste routines as Ctrl+C/Ctrl+V. Every DIT grid also receives a shared right-click Copy/Paste menu; hit testing focuses the clicked XtraGrid or VGrid cell before the menu opens, and an active DevExpress in-place editor is assigned the same menu so it cannot create a separate direct-bound paste route. Paste is disabled for read-only/live datasets and still performs the existing cell-level protection/rule checks before `ModelChangeManager` writes. Clipboard handlers and menu references are detached during section rebuild/disposal.

### Model-scoped change history and undo

Version 7.41 replaces the application-global V1 change display with ModelChangeManagerV2 and a model-scoped HistoryManagerV2. Successful interactive cell writes capture typed before/after workbook snapshots, including invariant formulas, and enter a linear per-model undo/redo journal only after calculation succeeds. Undo and redo validate that the current workbook cell still matches the expected snapshot, apply the whole change group in reverse/forward order, recalculate, and restore the group if any operation fails. New edits supersede the redo branch. DIT, FFR and Stress Test rectangular pastes are grouped as one history action; Ctrl+Z, Ctrl+Y and Ctrl+Shift+Z route to the active model, and the history window offers single-step and undo/redo-to-selected commands. Structural row/column operations remain deliberately non-automatic because safe reversal requires a complete domain-specific inverse across workbook-owned ranges and Transactional DB mirrors. The former change manager, history form and unused change-log manager are retained only as non-compiled text under Archive/History V1.

Version 7.42 makes DevExpress VGrid cell multiselect both consistently configured and visibly apparent. The shared VGrid setup enforces CellSelect mode and system selection colours, while DIT live/standard/Joint Venture and specialised FFR custom-draw paths reapply the selected-cell appearance after workbook-owned colours, fonts and number display have been sourced. This changes only the transient selection overlay; normal cells retain their XLSB appearance and edit protection.

Version 7.43 extends ModelChangeManagerV2 history to single-cell entities. DIT spin, text, combo and date controls now include blank-to-clear actions in the same typed journal used by grids, suppress dirty state during workbook refresh, and rely on central no-op filtering so calculation or undo refresh cannot manufacture duplicate history. The reusable ModelPosting text, date and combo controls and dormant extended-control posting hooks no longer assign workbook cells directly: committed values, including Nothing for a blank, flow through a shared single-cell posting service. ModelPosting controls refresh themselves after model-scoped undo/redo and restore workbook values after failed commits. Text and date editors commit on validation rather than on every keystroke; discrete combo selections retain immediate commit behavior.

Version 7.44 makes model undo/redo reliable while a DevExpress single-cell editor has focus. The DIT message filter now intercepts Ctrl+Z, Ctrl+Y and Ctrl+Shift+Z before the embedded editor can consume them as local text undo commands, while limiting handling to controls owned by that interface; reusable ModelPosting text, date and combo editors perform the same routing when hosted outside DIT. HistoryManagerV2 also exposes a fixed right-hand action column: applied rows show an undo icon, undone rows show a redo icon, and clicking an older row deliberately unwinds or reapplies the linear journal through that action. Superseded rows remain non-interactive.

Version 7.45 corrects the Global Assumptions single-cell undo path. Its controls are AbovoDETextEdit/DateEdit derivatives rather than ModelPosting editors, so those extended controls now route Ctrl+Z/Ctrl+Y directly to model history. DIT history refresh now reloads editable single-cell text, date, spin and combo controls as well as grids and read-only controls. A scoped suppression flag prevents the resulting EditValueChanged events from reposting the restored workbook value as a new action.

Version 7.46 corrects HistoryManagerV2 row undo/redo actions. Mouse hit-testing captures the group represented by the clicked action cell before its in-place ButtonEdit opens, and the command is deferred until that editor has closed. HistoryChanged notifications are suppressed while the manager is executing the requested stack transition, followed by one authoritative grid refresh; the row therefore changes from Undo/Applied to Redo/Undone (or back again) without an active editor restoring stale display data. Non-error no-action results are now reported rather than silently ignored.

Version 7.47 makes the restored workbook value propagate directly to live DIT single-cell editors. Writable text, date, spin and combo controls, including mapped-table text/combo editors, now hold a model-history refresh binding scoped to their source worksheet. After undo/redo each affected control reloads its own workbook cell under nested posting suppression, in addition to the containing DIT's general refresh. This removes reliance on the form-level deferred refresh reaching every dynamically-created editor and prevents refresh-generated EditValueChanged events from creating replacement history entries.

Version 7.48 makes the original DIT single-cell commit and its history record atomic from the user's perspective. The editor is marked clean and posting is suppressed while calculation performs its synchronous active-interface refresh, then the editor explicitly reloads the authoritative calculated workbook cell. ModelChangeManagerV2 now captures the history after-snapshot after calculation, so the Applied value, subsequent undo validation and visible editor cannot describe different workbook states.

Version 7.49 corrects the shared DIT undo/redo presentation refresh. History completion now refreshes its owning DIT synchronously after workbook snapshots, calculation and stack state are final. XtraGrid and VGrid controls backed by DevExpress UnboundSource reset that source at its existing row count before refreshing, invalidating values previously supplied through ValueNeeded; single-cell controls are reread in the same pass. The restored workbook state is therefore visible immediately across both entity editors and grids.

Version 7.50 closes refresh-time re-entry during undo and redo. DIT suppresses UnboundSource ValuePushed callbacks while XtraGrid and VGrid datasources are being reset or refreshed, and ModelChangeManagerV2 rejects any ordinary posting attempt while a history snapshot group is being applied. After calculation, every affected workbook cell is verified against its intended snapshot before the stack and log can report success; a re-overwritten cell now fails and rolls back the action instead of presenting a false Undone or Applied state.

Version 7.51 aligns workbook refresh with the DevExpress editor commit lifecycle. Single-cell Leave and grid/vertical-grid UnboundSource ValuePushed handlers now keep a posting-depth scope while ModelChangeManager writes and calculates. Any calculation-driven or explicit DIT refresh encountered in that scope is coalesced through BeginInvoke and runs only after DevExpress has completed the current edit message, preventing its pre-refresh editor buffer from restoring the previous visible value after a successful journalled change.

Version 7.52 separates model-history notification from the workbook transaction. HistoryChanged subscribers are invoked individually under exception isolation, so a stale, disposed or otherwise failing presentation subscriber cannot escape back into ProcessResolvedChange after the change group has been committed. This prevents the former inconsistent outcome where the history row remained Applied but the transaction catch restored the workbook's old value; one subscriber failure also no longer prevents the remaining DIT, grid and single-cell refresh subscribers from receiving the notification.

Version 7.53 adds keyboard progression for DIT XtraGrid defining-row editors. Pressing Enter in a ColumnInplaceEditorHelper validates and commits the active DevExpress header editor, then moves focus to the next visible column-header editor in the same BandedGridView after any synchronous workbook calculation or layout refresh. The final editor closes normally when no later header editor exists. Ordinary grid cell editors and VGrid row-header editors are unchanged.

Version 7.54 corrects ColumnInplaceEditor Enter traversal for banded headers. Navigation now uses each BandedGridColumn's source-order AbsoluteIndex rather than the unreliable band-relative VisibleIndex, and the temporary DevExpress editor explicitly disables EnterMoveNextControl so the form cannot transfer focus to the next grid while the helper is selecting the following header editor.

Version 7.55 preserves ColumnInplaceEditor traversal across workbook calculation and rule refresh. Successful combo/date header changes now queue one deduplicated advance request from the DIT change handler after UpdateAllRules; Enter without a changed value uses the same request. The helper closes any surviving current editor, forces the post-layout header paint to reacquire authoritative bounds, and focuses the next source-order header editor. This applies to accepted keyboard and popup/mouse edits in XtraGrid only.

Version 7.56 separates XtraGrid defining-row typing from commit. ColumnInplaceEditorHelper no longer posts on each RepositoryItem EditValueChanged event, allowing multi-character values such as 13 to be entered without a transient 1 being calculated and advancing focus. Enter and Tab validate, commit through the existing DIT ModelChangeManager handler, and move to the next header editor; Shift+Tab moves to the previous header editor. Genuine focus loss commits while respecting the control selected by the user. VGrid behavior is unchanged.

Version 7.57 normalises the in-column editor display sentinel `<Blank>` to Nothing before creating its DataChangeEvent. ModelChangeManager therefore clears the authoritative target cell through its established typed clear-contents path instead of storing the literal sentinel text. Successful UI state, history and rule refreshes receive the resulting blank value consistently; the rule applies to both XtraGrid and VGrid defining-row handlers that expose the sentinel.

Version 7.58 adds a most-recent-period fill action to DIT XtraGrid defining-row editors. Double-clicking a ColumnInplaceEditor copies a pre-write snapshot of the nearest populated defining column to its left into the editable cells beneath the selected column. Read-only, calculated, dummy, locked, spacer and control cells are skipped; all accepted writes use ModelChangeManager and appear as one grouped undo operation before the normal rules, calculations and interface refresh run. VGrid defining-row editors are unchanged.

Version 7.59 preserves the legacy grouped-sheet dependency semantics when Development identified or multi-year columns are expanded. DevExpress performs the seven worksheet insertions sequentially, so `Dvpt Component Depn` is now expanded before its dependent `Dvpt NonCash` sheet. This prevents freshly copied NonCash formulas from being shifted onto existing multi-year depreciation data by the later source-sheet insertion. A read-only comparison of the reported five-column failure showed that all fourteen Development Transactional DB mirrors had expanded correctly; the sole material error was 160 new NonCash formulas referring five columns to the right. Replacing only those references in a disposable copy and running Excel full calculation restored both the Statement of Financial Position and Transactional DB Check Sheet results to `OK`.

Version 7.60 completes the Other Fixed Asset semantic event conversion. The DIT add/delete dialog remains the sole prompt and its `RequestedRecordCount` now flows through the shared rule helpers into `OFA_RECORDS`; the legacy OFA handler no longer opens a second add-record dialog with its own default. Negative DIT adjustments also use the shared delete-last rule path, while direct selected-row commands retain the established selected-record fallback.

Version 7.61 adds an editable, context-sensitive Summit HTML help library. Every DataInterfaceTemplate now exposes a tagged Help WindowsUIButton that routes the current GroupStructure ID, ChildStructure ID, active tab and worksheet to an integrated modeless help viewer. The initial searchable reference covers all 217 configured interfaces and is generated from `Structure.xml`, the workbook User Guide and 777 legacy cell comments read from `Z:\Sandbox\TestFileClean.xlsb` with Excel macros/events disabled and without saving. Generated facts remain separate from `Help/data/overrides.js`, where durable client-authored HTML can be maintained without being overwritten by regeneration. The Help folder is copied to both application configurations, and the viewer also provides a direct Open Help Folder action.

Version 7.62 routes the existing bottom-left Help button on `FormMainScreen` to the local Summit HTML help home page through the shared `HelpManager`. This replaces the obsolete external website launch while retaining the established `GetHelp` WindowsUIButton tag and shared modeless viewer lifecycle.

Test Release 1.00 resets the client-facing Summit version and configures a repeatable offline ClickOnce package for initial functional testing. The separate installed product identity is `Abovo Summit Test`, the application/deployment version is `1.0.0.0`, automatic updates are disabled, and the bootstrapper prerequisite now matches the application target of .NET Framework 4.8. `Structure.xml` is explicitly included at the installed application startup root and the complete editable `Help` tree is explicitly included beneath it. `Tools/Publish-ClientTest.ps1` refuses to overwrite an existing versioned destination, invokes the project ClickOnce target, validates the required staged files and application-manifest entries, writes client installation notes, and produces the delivery ZIP. The available legacy test certificate is not installed in the certificate store, so this initial package is deliberately unsigned and the client README explains the expected Windows `Unknown Publisher` warning.

Test Version 1.01 restores the first `Funder` tab to both Funding Assumptions V1 (CSID 33) and Funding Assumptions V2 (CSID 138). Each tab presents the workbook's vertical `Funders` and `Facility` named ranges as one editable, non-pivoted `MergeAcross` grid. The `Funders` range is the NRRI expansion master, so an Add Lines operation inserts complete worksheet rows and keeps the aligned `Facility` range synchronized. Read-only inspection confirmed both contract ranges contain seven rows on `Funding Assumptions` (`B35:B41` and `D35:D41`), and Debug/Release builds both passed with the regenerated `Structure.xml` copied exactly to their startup folders.

Test Version 1.02 assigns the visually first row of both Funding Details vertical grids a dynamic combo editor backed by the workbook `Funders` named range. The editor uses the existing `Rep_Funders` resolver, so its choices are reread from the authoritative workbook rather than duplicated in application code.

Test Version 1.03 reconciles both Interco Funding Assumptions tabs to the authoritative workbook. Loans Received uses `IC_IntercoFunding_01` with `Rep_IntercoFunding_010/030/050/070`; Investments Made uses `IC_IntercoFunding_02` with `Rep_IntercoFunding_020/040/060/080`. This removes the erroneous Capital Grant and loan-side references from the investment tab, restores monetary formatting to Drawdowns, fixes the description/opening columns during horizontal scrolling, and gives all drawdown, repayment, investment movement and interest-rate bands workbook-backed date editors and expansion actions through `DateInterF01/02/03`.

Test Version 1.04 makes the first repeated date in every Interco Funding movement and interest-rate band editable. Each band now creates a `ColumnInPlaceEditor` date editor from record zero instead of suppressing the first header editor as a legacy read-only initial line.

Test Version 1.05 prevents the model-scoped History Manager from calling `BeginInvoke` before its WinForms handle has been created, or while that handle is being destroyed during model close. Pre-display notifications need no queued refresh because `ShowForUser` refreshes from the complete journal before showing the window; close-time races are safely ignored after the workbook transaction has already committed.

Completed automated/static validation:

- Exact source/library hash comparison
- Read-only Excel workbook/name/range checks
- All canonical Transactional DB mirror geometries
- Capital Grant assumptions/workings/Transactional DB five-column expansion geometry
- Disposable five-column Development repair recalculated in Excel with Financial Position and Transactional DB checks both returning OK
- V1/V2 datasource field and period contract review
- Debug and Release MSBuild passed with zero build errors on 24 August 2026
- Generated Summit Help data parsed with 3 groups and 217 interfaces; a contextual Global Assumptions page rendered successfully through a hidden WinForms WebBrowser with its interface and field guidance present
- Release ClickOnce publish produced `Abovo Summit Test` version `1.0.0.0` with 89 files; exact SHA-256 comparisons confirmed the staged `Structure.xml` and all six Help files match their repository sources
- Application manifest contains the startup-root `Structure.xml` and nested Help paths; the delivery ZIP contains `setup.exe`, deployment/application manifests, client test README and all staged application files
- Client delivery archive `Z:\Sandbox\Deploy\Abovo-Summit-Test-1.00-ClickOnce.zip` SHA-256 is `63691F5F0C5CEDE7FC8DF31CF7E0DD32BBF734A4034935A3E3EF3EA41C504219`; Windows PowerShell reflection confirmed the compiled UI version text is exactly `1.00`

Required manual integration validation remains:

- Open Funding Assumptions V1 and V2, confirm `Funder` is Tab #1, edit both columns, add rows, and verify `Funders` and `Facility` remain aligned and feed the Funding Details dropdowns after calculation and reopen
- Open several Assumptions, Workings and Outputs DIT instances, select different tabs, and confirm Help opens the matching interface and scrolls to the active section; edit one `Help/data/overrides.js` entry and confirm it takes precedence after reopening Help

- Open Funding Assumptions V2, verify all four tabs, confirm Funding Details and its Opening Balance line remain fixed while scrolling, and exercise a Commitment Fees date-header add/edit/remove round trip
- Open the TestFileClean master in Summit and open Analysis V1 and V2 in both orders
- Add/remove a record in each structural domain and verify `Transactional DB` mirrors and analyser refresh
- In particular, add five Capital Grant records and verify both `Capital Grant Assumptions` and `Capital Grant Workings` expand by five records
- Verify Capital Expenditure, OFA, Repairs, Housing Components, Development Details, Development Multi Year, Journals and Stock Conversion use their declared semantic rules rather than the generic single-range path
- Add five Development Details records from the clean master and confirm the Financial Position and Transactional DB Check Sheet rows remain OK; repeat for Development Multi Year
- Close a passing model and confirm the normal Save/Discard/Cancel flow follows the completed full calculation
- Trigger a Check Sheet failure, confirm Cancel returns to the open model, and confirm OK requires a differently named XLSB copy before close
- Save a copy in Summit, open/save/recalculate it in Microsoft Excel/VBA, then reopen it in Summit
- Exercise Stress Test scenario generation/import and sensitivity expansion on a disposable copy
- Paste mixed text, numeric, percentage and blank rectangles into an editable DIT vertical grid and verify visual transposition, locked-cell skipping, recalculation and Excel round-trip
- Repeat paste checks in FFR Workings, FFR Inputs Adjustment Statement, FFR Key Definitions and each editable Stress Test grid; verify the read-only FFR/Stress output grids remain copy-only
- In a DIT XtraGrid and VGrid, verify toolbar, keyboard and right-click Copy/Paste produce identical results; repeat with an in-place editor open and confirm right-click targets the clicked cell and read-only/live grids expose Copy with Paste disabled
- Edit text, numeric, date, percentage, blank and formula-backed cells, then verify Ctrl+Z/Ctrl+Y and the History V2 buttons restore exact workbook values and displayed formulas after recalculation
- Paste rectangles in DIT, FFR and Stress Test and confirm each paste is one undo group; create a new edit after undo and confirm the discarded redo branch is marked superseded
- Modify a journalled cell externally before undo and confirm conflict detection refuses to overwrite it; confirm structural add/remove commands are not presented as automatically reversible

## Contract XLSB health and calculation audit - 24 August 2026

At the time of the 24 August audit, its then-current authoritative workbook and repository copy were byte-for-byte identical. A reserved-capacity prototype briefly superseded it on 3 September 2026, but the client withdrew that design on 4 September 2026. The current authoritative master is the validated pre-capacity workbook identified above.

The workbook is structurally healthy and natively fast on the audit workstation: 638,360 formula cells completed a full dependency rebuild in 2.152 seconds and warm full calculations averaged about 0.42 seconds. No broken defined names, external formula references, workbook connections, circular reference, or true full-column calculation formulas were found. Broad formula replacement is therefore not justified.

The primary confirmed integration issue is the Summit `PMCOST` parameter contract. The XLSB deliberately calls eight compatibility positions, but only seven inputs affect the result because `FinalYear` is unused. Summit accepts positions 0 through 7 while exposing seven `ParameterInfo` entries. Preserve the eight-position workbook signature, document the unused position, correct the metadata, and require Excel/VBA-versus-Summit parity across all 380 `PMCOST` and 380 `RESPCOST` calls before changing evaluation logic.

The main workbook robustness risk is positional dependency: 35,920 formulas use 3-D worksheet references. Structural commands must validate the exact membership and order of every bounded sheet block. Multi-area names, used-range cleanup candidates, volatile `CELL` formulas and the single unguarded `Hidden - Tenure Totals Start!B6` lookup are targeted review items, not evidence for a wholesale redesign.

Future workbook, calculation-engine, Transactional DB, custom-function, structural-range and validation work must first review:

- `Library/Contract_XLSB_Audit_2026-08-24.pdf`
- `Library/Contract_XLSB_Audit_2026-08-24.md`
- `Library/Contract_XLSB_Audit_Evidence_2026-08-24.json`

## Survey Input interface mapping - 16 September 2026

Read-only inspection of the current Demo master confirmed five 40-year stock-condition blocks on `Stock Condition Inputs`. `Rep_SCond_01` through `Rep_SCond_05` each cover 40 rows by 13 cost-category columns; `StockCondCats` is the horizontal 13-category header range. The year columns are `VertYears` (`A9:B48`) and `A55:B94`, `A101:B140`, `A147:B186`, `A193:B232` respectively. Each block's calculated total is the immediately following Q column. Archetype labels are in `A6`, `A52`, `A98`, `A144`, and `A190`.

The original Survey Input `MergeDownAndPivot` definition derived its dataset row count from the two-column `VertYears` range, producing an incorrect two-row interface. The corrected `Structure.xml` uses `MergeAcross` and one workbook-backed section per block. XML parsing and Debug/Release builds passed; visual interaction, edits, save/reopen, and Excel/VBA round-trip remain manual checks. The XLSB was not modified.

## Model Manager definition trial - 19 September 2026

Summit 2.33 adds a Model Manager entry alongside Compare/Upgrade. It stores human-reviewed draft definitions and three-file fingerprints, with an explicit Generic flag and a separate custom XML part embedded only into newly named workbook copies. This is not yet a bespoke migration executor. Password access is explicitly disabled for the trial; the configurable live gate fails closed without a valid verifier. Normal model open reads optional metadata without modifying cells or applying rules. See `Library/Model_Manager_Trial_2026-09-19.md` for architecture, boundaries, output paths and test plan. Multiple custom XML parts were verified through DevExpress 25.2 and Excel save/reopen on Stori and Blank copies; original worksheet/VBA/SharePoint parts were hash-verified unchanged by the embedding operation. Macro execution and interactive UI remain manual tests.

## Analyser chart trial - 21 September 2026

Summit 2.47 adds an optional native line chart to each analyser statement, using
the existing grid's grouped totals and leaf values with Live/Snapshot/Differences.
Right-click drills to the next data level; Up/Overview and a dropdown support
return navigation and overlapping zero lines. No extra binding, workbook write
or calculation occurs on chart navigation. Native Blank/Demo tests cover numeric
parity, actual legend hit testing, mode/path/grid-expansion retention, frozen
snapshots after typed edits, all 40 difference periods and deferred-refresh safety.
Source hashes are unchanged; physical DPI and interactive structural mutation
remain acceptance checks. The existing unclassified BS hierarchy is surfaced, not
invented. See `Library/Analyser_Chart_Trial_2026-09-21.md`.

## Structure Manager authoring trial - 20 September 2026

Summit 2.46 adds an isolated three-pane authoring window: structure tree, native read-only workbook/property area, and real read-only DIT preview. It retains the existing access gate and metadata-rule editor, uses a private temporary workbook copy in a separate process, and saves only newly named draft XML files. It does not publish production structure, alter master workbooks, expand ranges or execute bespoke upgrades. Debug/Release builds, Blank/Demo native authoring fixtures, ordinary FFR/Stress history and existing Model Manager regressions passed with unchanged source hashes. Physical DPI, interactive selection/keyboard and publication/Excel-VBA acceptance remain manual. See `Library/Structure_Manager_Trial_2026-09-20.md` for precise scope, risks and test plan.

## Mapped table / Check Sheet trial - 20 September 2026

Summit 2.34 batches the Accounts mapped-table font layout pass (151 controls; approximately 293 ms reduced to 11 ms in the isolated fixture). Structure revision 1749 adds a read-only Check Sheet tab under Global Assumptions using an optional compact MappedTable range projection. Column G markers resolve worksheet targets from H and offer existing Summit interfaces or the model's global spreadsheet viewer. Both authoritative masters passed 399-cell display, 27-link resolution, selection/refresh and conditional error-colour checks with unchanged file hashes. See `Library/Mapped_Table_Check_Sheet_2026-09-20.md` for implementation boundaries, measured phases and the remaining interactive/DPI/round-trip test plan.

Summit 2.36 / structure revision 1750 adds explicit Worksheet and Summit interface destination columns. Check Sheet remains read-only except for the workbook's five unlocked Yes/No validated inputs, using native combo editors and ModelChangeManager for writes/calculation/history. Both authoritative masters passed valid/invalid edit, dirty marking, undo/redo, active-editor refresh, protection preservation and unchanged source hash checks. Visible Analysis and Funding Assumptions names drop V2 without changing IDs or implementation classes. File-instance buttons now place Stress Test before Spreadsheet and use a profile-specific HA BP badge caption; nine width/font layout variants and the DSA caption passed the isolated fixture. Physical-DPI, interactive navigation and Excel/VBA save/reopen acceptance remain manual.

## Standalone FFR / Stress Test history - 20 September 2026

Summit 2.37 adds direct model-scoped Change History access, child-editor undo/redo shortcut routing and post-history refresh to both standalone forms. FFR was already non-modal; Stress Test now opens non-modally too. User close still hides and retains each instance, while model close disposes both and detaches their event/message-filter bindings before releasing the workbook. Demo and Blank fixtures verified journalled FFR and Stress edits, undo/redo presentation, a native FFR VGrid active-editor case, hide/reopen persistence, deduplicated Interface History and final cleanup with unchanged source hashes. Debug and Release builds passed. Bulk stress commands (mode changes, capture/import/generation and structural actions) remain outside automatic undo; they require domain-level reversible snapshots. See `Library/Standalone_History_2026-09-20.md` for boundaries and the manual keyboard/multi-model/Excel-VBA test plan.

## Presentation layout trial - 20 September 2026

Summit 2.38 / structure revision 1751 removes the Stress Test width cap, fits native command captions and toolbar glyphs to the established display scale, aligns initial file-details typography, and adds optional short XML navigator captions without changing routing or worksheet definitions. DIT and Analyser provide a host-window-scoped Hide panels / Restore panels toggle. Debug/Release builds, native layout fixtures, non-cumulative font scaling and the Demo standalone history regression passed with unchanged source workbook hashes. Physical 5k/client DPI, uncommon command rows and side-by-side manual acceptance remain outstanding. See `Library/Presentation_Layout_Trial_2026-09-20.md`.

Summit 2.39 follows up the user's Stress Test screenshot: recreated covenant summaries, rebound results and regenerated chart titles now retain the input grid's display font. The results grid uses its available width. The WindowsUI toolbar is left-aligned, explicitly captioned and content-height-sized, with Home first and Change History last. Refresh and native hit-target fixtures passed at 100/150/200/100% user scale, plus the existing file-instance layout and standalone-history regression. This is presentation-only; original workbook hashes are unchanged. Physical mixed-DPI and client-screen acceptance remains outstanding.

Summit 2.40 makes Stress Test's automatic density follow its client width as well as its monitor, preserving explicit user font preferences and the approved wide-window scale. The toolbar/content bounds, capture editor rows, summary stacking, readable scrolling input columns and chart markers adapt to restored windows. Debug/Release builds, native 1280/1792/2800/5000-width and maximise/restore checks, refreshed typography, native hit targets, the file-instance layout fixture and standalone history regression passed. Source workbook hashes remain unchanged; physical mixed-DPI and manual editing while resizing remain acceptance checks. See the 2.40 section of `Library/Presentation_Layout_Trial_2026-09-20.md`.

Summit 2.41 applies owning-window density to GIT/DIT/Analyser, bounds navigator/sidebar widths, moves the DIT toolbar outside its designer-scaled table row, and increases sidebar HTML text to 9.5pt with a browser-compatible compact File Details layout. The editable single-percentage creation path now supplies TargetWorksheet/TargetCell; late refresh/post callbacks are guarded after interface resource release. Debug/Release builds and the native group fixture passed widths 1280/1900/2800/5000, actual maximise/restore, exact hide/restore state, resizing while hidden, the real Stock Survey Allocation percentage refresh, unbound/disposed refresh, and refresh after resource release. At 1900 pixels the panels were 315/340 pixels and the toolbar 74 pixels high. Browser DOM confirmed 9.5pt, and the source Demo SHA-256 remained unchanged. Physical 5k/mixed-DPI acceptance and an interactive percentage edit/undo/close/reopen remain manual. See the 2.41 section of `Library/Presentation_Layout_Trial_2026-09-20.md`.

## Analyser chart controls and Balance Sheet plan - 21 September 2026

Summit 2.48 moves the per-statement chart/grid toggle to an icon-only WindowsUI main command with a separator, increases line thickness from 2 to 3, and provides a native right-click Go Up / Drill down to [series] menu. Sidebar accordion headers now match navigator blue/white while retaining compact content fonts and expansion state. Debug and Release builds passed; native chart regressions passed on Release Demo and Debug Blank, and the focused sidebar fixture passed. The broader GroupOnly layout run still flagged the DIT toolbar height at a 1280px restored width; physical DPI and narrow-window acceptance remain outstanding. See `Library/Analyser_Chart_Trial_2026-09-21.md` for the explicit validation boundary.

The separate Balance Sheet investigation confirmed its current transaction-table feed is incomplete, while the worksheet's SOFP output reconciles with Trad View. A read-only drill reconstruction matched all 37 primitive lines over 40 years in both Demo and Blank. The proposed repair uses a separate read-only statement adapter and additional approved regions on existing snapshot sheets, not extra live worksheets or new synchroniser sizing rules. Snapshot persistence, cache invalidation and the existing structural deferral lifecycle need integration. No Balance Sheet repair, workbook schema change or calculation change was implemented in 2.48. See `Library/Analyser_Balance_Sheet_Repair_Plan_2026-09-21.md`.

Summit 2.49 corrects the reported blank sidebar HTML lifecycle, missed by the 2.48 header-only test. Latest desired documents are retained until browser readiness and updated synchronously without repeated blank-page navigation. Native DOM and layout regression coverage now includes initial content, repeated/rapid refresh, resized/hidden/restored and delayed second-window content, and stable grid widths/container heights when records arrive. Sidebar animation is disabled and message/history repaint/layout is batched without repeated column best-fitting. Analyser charts add native two-axis wheel zoom, Ctrl-drag panning and Reset zoom; native wheel events and the existing chart regression passed. Debug/Release builds passed; original workbook hashes are unchanged. Client 5k visual acceptance, the narrow DIT toolbar issue and the proposed Balance Sheet repair remain outstanding.

Summit 2.50 increases analyser line thickness to 5 and point marker size to 8 for easier targeting. Native off-centre point hit testing, unchanged left/right-click behaviour and the full Release Demo chart regression passed with the source workbook hash unchanged. Debug/Release compilation passed (Debug initially staged separately while Summit was running). Client 5k targeting acceptance remains manual; no workbook, calculation, sidebar or Balance Sheet behaviour changed in this follow-up.

## Structural audit and repair trial — 23 September 2026

Summit 2.80 repairs verified OFA/Repairs insertion anchors/templates, hidden-template and leading-column deletion boundaries, shared three-row minima, a distinct-source service-charge mirror collision, and the Repairs Include companion name during explicit structural commands. The AGL one/many/populated inverse and deletion-boundary checks, Blank/Demo regressions, and independent macro-disabled Excel comparisons are documented in [Structural_Audit_Repairs_2026-09-23.md](Structural_Audit_Repairs_2026-09-23.md). Four Excel round-trips preserve 1,762 names and 335 VBA module source identities. Original AGL and both masters remain unchanged; no load migration was added. Debug/Release build; client DIT and trusted Excel/VBA financial acceptance remain separate.

## Idle integrity trial — 22 September 2026

Summit 2.67 adds an opt-in Integrity settings tab and revision-local staged checks after a configurable interval and at least two minutes without Windows-session input. Pending calculations/dependency rebuilds and their necessary result refresh finish atomically; user activity defers subsequent stages, never interrupts running work. Read-only checks cover Check Sheet results, names, supported Transactional DB mirror sizing, cached cell errors and existing XLSB export hazards. It does not resize ranges, repair formulas, save files, run VBA or concurrently access the live workbook. Native Debug/Release safety tests, private Blank/Demo state preservation and Undo/Redo, recovery regression and save-state regression pass. Reported native chart/menu errors still require classification, and physical input, multi-model/client performance and Excel/VBA acceptance remain outstanding. See `Library/Idle_Integrity_Trial_2026-09-22.md`.

## SpreadsheetGear full Funding research — 24 September 2026

The isolated ten-column Funding port covers all 32 linked sheets and eleven TDB mirrors, with actual embedded Excel/VBA as its comparator. Gear takes about 1.5 seconds for the native command versus 13.4 seconds for the single Excel reference pass, excluding load, explicit rebuild, save and reload. Copy versus AutoFill shows no worthwhile full-column difference. Native DevExpress reload adds 28.4 seconds.

Do not integrate this raw helper: the serial insert leaves 12,050 3-D-reference formula differences versus Excel grouped insertion. A synthetic test confirms that serial Excel insertion behaves the same as Gear; a grouping/reference-preservation adapter is required. Save-only Gear serialization preserves VBA bytes and audited populated-cell fills/locks, but drops custom XML and dynamic-array metadata. Excel can recalculate the unchanged save-only control correctly; it reproduces the erroneous financial results of the structurally inserted file, so later recalculation is not a repair. Production and originals are unchanged, with live Summit staging/UI rebind deliberately gated rather than claimed as tested. Full evidence, limits and next decision: `Library/SpreadsheetGear_Funding_Full_Trial_2026-09-24.md`.

## Persistent Gear/native-worker research — 24 September 2026

The isolated two-process prototype now accepts Excel or DevExpress as a persistent native worker, with a Gear working copy, durable revisioned journal, save barrier and private candidate replacement. This is not integrated into Summit. The user distinguishes build/update (structural/bulk work) from review (small-edit implication checks); optimise those workloads independently without changing authority, locking, history or Excel/VBA round-trip requirements.

The Excel-worker Funding checkpoint corrects the prior Gear 3-D discrepancy: all 38,108 established output probes match the independent Excel/VBA oracle, and saved dynamic-array ranges/counts match. One development-machine pass took about 0.5 s per warm Gear edit loop, 6.4 s for worker save and 29.9 s for the full Funding/checkpoint/rebase barrier. These are not live Summit UI or minimum-spec results. Value-only edits can expand dynamic arrays that Gear fails to expand, so structural-only read-back needs an eligibility rule or fallback. The DevExpress path stops before its full Funding test because Gear rejects its saved checkpoint; Excel opens it normally. Original workbooks and production code/version are unchanged. See `Library/Spreadsheet_Two_Engine_Prototype_2026-09-24.md` for evidence, native VBA/XML caveats and remaining gates.

Follow-up diagnosis isolates the Gear rejection to object import: the unchanged DevExpress checkpoint opens with `ReadObjects=false` and VBA retained; all 38,108 calculated before/after probe cells match. This supports a possible Excel-free calculation-copy adapter, not full structural compatibility or authoritative Gear serialization. Existing DevExpress-only Summit remains the baseline. See `Library/SpreadsheetGear_DevExpress_Import_Diagnosis_2026-09-24.md`.

Second prototype checkpoint: all Gear mirror loads now use the calculation-only importer and reduced copies cannot save. The complete DevExpress Funding path now runs, but fails the independent audit: 3,729 TDB array declarations disappear despite retained formula text; native reload also loses their dynamic identity and Gear shows three TDB check failures. An immediate source-aware row-copy guard did not repair the eventual loss. Optional Excel again matches all 38,108 probe values and array geometry. Full Funding/save/rebase takes about 29 seconds with Excel versus 172-177 seconds with DevExpress in these runs. Explicit native range-result fallback handles unobstructed spill growth and rejects late results after newer edits; native calculation exceptions block subsequent reads/saves. These are isolated transport/engine tests, not a production adapter, client acceptance or authority to overwrite customer files. Production 2.98 and original XLSB hashes are unchanged. See `Library/Spreadsheet_Mirror_Projection_Trial_2026-09-24.md` and `Library/Developer_Context_Blueprint_2026-09-24.md` for evidence and the precise continuation plan.

Third prototype checkpoint: a small workbook reproduces stale dynamic-array registrations after native row insertion (also bounded InsertCells). A batched native guard now preserves the complete AGL Funding checkpoint: 38,108 probe values and 56,458 TDB arrays match the independent Excel/VBA oracle; native and macro-disabled Excel reopen checks pass within documented limits. The complete persistent worker passes save/revision/restart tests, but the structural barrier costs 302.73 seconds, including 121.02 seconds of array handling. Original AGL XLSB loads as legacy arrays while the converted XLSM loads as dynamic arrays, so this is not evidence by itself of the same production XLSB defect. Production 2.98 and original/master hashes remain unchanged. See `Library/DevExpress_Array_Insert_Diagnosis_2026-09-24.md` for the full checkpoint and outstanding performance, spill eligibility, structural coverage and production publication gates.

## Spread.NET primary-engine evaluation — 24 September 2026

25 September continuation 2n: native FFR/dashboard/comparison/integrity consumers and expanded direct-mutation guards are implemented. Isolated builds now select compatible Excel at actual model opening, with Options status and ordinary DevExpress fallback; production 2.98 is unchanged. Whole private Demo opening/edit/recovery/Save/Save As/Undo/Redo/close passes, as do generated reader/guard/selection tests. Effective-appearance transport is still slow and final qualification/versioned delivery remain outstanding. See `Library/Excel_DevExpress_Engine_Stage2n_2026-09-25.md` and the Stage 2 work plan; this is not a completion claim.

25 September continuation 2g: the existing change manager can internally own native atomic value batches and Undo/Redo, with real dirty/history state. Effective appearance is transferred with values at a calculation generation; Excel DisplayFormat is required to reproduce DevExpress Cell.Fill's conditional behavior. Shared DataManager, mapped-table and Check Sheet/override readers have a selected-owner facade, with normal DevExpress behavior retained when unbound. Generated native tests prove Check Sheet/company-warning and defining-date/amount lock coherence. This is partial integration: editor admission, remaining direct readers, live opening/settings and continued Save/dirty lifecycle are still gated. Stage 2 is not complete and normal 2.98 is not switched. See `Library/Excel_DevExpress_Engine_Stage2g_2026-09-25.md`.

25 September continuation 2f: opted-in candidates can carry current immutable display history from the existing change manager. History is bound to exact model/user/calculation and engine/source revisions, serialized through the existing recovery-history writer, and audited as an exact ZIP-part delta. Other XML, VBA, dynamic metadata and worksheets are preserved. A successful history receipt is evidence only and never marks the live model clean. Ordinary Save, authoritative live edit/Undo ownership and current structural/XML coupling remain unconnected; production 2.98 and original workbooks are untouched. See `Library/Excel_DevExpress_Engine_Stage2f_2026-09-25.md` for validation and limits.

25 September continuation 2e: explicit, target-bound recovery inspection now recognizes interrupted terminal publication, offers original-only restoration to a vacant name, and preserves competing files and unsaved candidates. Successful in-memory publication receipts can reopen exact saved bytes/provenance using unchanged engine/security options, with a new session identity and revision-zero baseline. No live dirty acknowledgement, current history XML serialization, automatic startup mutation or client release is enabled. See `Library/Excel_DevExpress_Engine_Stage2e_2026-09-25.md` for current validation and remaining gates.

25 September continuation 2d: a separate default-off terminal publication trial consumes the native session, verifies the current source and publishes only through no-overwrite handle renames, retaining the previous bytes and recovery records. New-name Save As is supported; arbitrary destination overwrite is not. The source name is briefly absent between the two replacement renames, so startup journal recovery, ordinary Save/rebase and production history integration remain release gates. The observed AGL secondary provenance stream is preserved unchanged, not used to grant trust. Production 2.98 and original workbooks are unchanged. See `Library/Excel_DevExpress_Engine_Stage2d_2026-09-25.md`.

25 September continuation 2c: separately opted-in same-format candidate export now checks session/revision, security marking, native cell/output read-back and package preservation. Value-only candidates retain unchanged custom XML companions and the opaque source VBA image; dynamic metadata is verified rather than grafted. Disposable AGL XLSB/XLSM candidates calculate equivalently after reopening in the other engine across 45,586 checked positions. Source publication, current history XML, live Save/dirty integration and permanently hung owner recovery remain unimplemented. Production 2.98 and originals are unchanged. See `Library/Excel_DevExpress_Engine_Stage2c_2026-09-25.md` for evidence, scope and limitations.

25 September continuation 2b: explicitly opted-in in-memory value editing now provides expected-before snapshots, native full calculation/read-back at a single revision, typed receipts and compensated failure/cancellation. The source stays read-only and no Save API or live change-manager/UI integration is enabled. Existing fill-based locking is unchanged. Current evidence and remaining gates: `Library/Excel_DevExpress_Engine_Stage2b_2026-09-25.md`.

25 September continuation: checkpoint 2a adds a detached read-only native-grid result source and bounded owner-thread cleanup waits. The scalar custom-calculation callback is not a safe general projection for existing/new dynamic spills. Production 2.98 remains unchanged. The user explicitly approved three-way Stage 3 structural comparisons (DevExpress .NET, managed Excel object model, Excel/VBA). Current evidence and protocol: `Library/Excel_DevExpress_Engine_Stage2a_2026-09-25.md`, `Library/Structural_Engine_Comparison_Protocol_2026-09-25.md`.

Current implementation direction was subsequently changed by explicit user approval: prefer compatible installed Excel automatically, with configurable DevExpress fallback and existing macro security respected. `Services/WorkbookEngines` now provides a separately tested read-only STA session foundation; it does not switch production 2.98 or yet own live edits/saves. See `Library/Excel_DevExpress_Engine_Stage1_2026-09-25.md` for authority, code-only checkpoint publishing, tests and staged release gates. The Spread/Gear evaluations remain historical compatibility evidence.

The isolated same-process Core/WinForms 19.2.0 trial is not a production migration candidate in its tested configuration. Three simple synthetic runs pass (16.720 ms warm full median; 8.699 ms edit calculation), but correctly registered AGL custom functions do not resolve the other model-output discrepancies. DocumentCaching preserves VBA and Summit XML payload bytes, yet the AGL save is rejected by both independent DevExpress load and normal macro-disabled Excel open; its unchanged input opens normally. Small insert/spill cases also fail natively while their exported files recalculate correctly in Excel. Grouped-sheet/3-D insertion equivalence remains unestablished. Extended business-command benchmarking is deferred pending vendor clarification; neither a worker nor package repair was used. Production 2.98 and original/master files remain unchanged. See `Library/SpreadNET_Primary_Engine_Evaluation_2026-09-24.md` and `Tools/SpreadNetTrial` for accepted synthetic metrics, excluded setup pilots, reproducible cases and remaining gates.

## Balance Sheet repair and Funding navigation trial — 21 September 2026

Summit 2.51 implements the approved Balance Sheet plan with a read-only SOFP adapter, native TreeList, shared chart hierarchy and numeric hierarchy export. Live headlines remain workbook-owned; detail explains openings plus cumulative contributions using validated formula families. Persistent BS bundles are created only by an explicit snapshot command on the two existing dedicated worksheets. Old SOCI/Cashflow snapshots are preserved as a supported case without BS data; no new worksheets, load-time migration, source-master edits, live business formulas or synchroniser sizing changes were introduced.

Private Demo and Blank fixtures match all 1,886 headline/opening values to Trad View. All hierarchy branches reconcile, including an injected reserve extra-span row satisfying both criteria. Snapshot collision/protection, frozen persistence, opening/classification changes, missing/duplicate/unsupported mapping, and real Cash Journals five-row insert/delete/recapture tests pass. Stori's 1,845 financial values agree; its 41 Check differences are the existing rounded TDB versus unrounded Trad result. Stori lacks the dedicated snapshot sheets, so no schema is silently added. Native charts, BS figures state, every BS export value and typed-edit Undo/Redo pass. Native Excel read-only SaveCopyAs/reopen preserves the captured hierarchy/values, sheet order and names; 337 VBA module source hashes remain identical, although Excel rewrites the binary container. Macros and Excel calculation were deliberately disabled in that test.

The Funding investigation reproduced a delayed whole-grid focus scroll after refresh. DIT pages now retain their viewport on grid focus; explicit keyboard navigation reveals the destination editor. Refresh preserves cell and scroll state, header commits are deferred until leave/navigation, and Tab/arrows route through permitted native/header cells. Native Release and Debug Funding tests cover actual active-editor key events, posted values, lower header visibility, rejected validation and continued editing. Adjacent CPI/RPI grids and existing Rent/Voids banded-header navigation pass. MultiEditorRow/custom joint-venture navigation remains native.

Debug and Release builds pass. Original source hashes and XML routes remain unchanged; git diff whitespace checks and index JSON validation pass. A rebuild initially met the private fixture's executable lock; after the fixture exited, the final build was rerun. The user will perform functional checks and arrange accountant financial review. The wider Development/Funding/component structural matrix, interactive Excel/VBA calculation, extensively populated x86/x64 performance and client mixed-DPI/keyboard acceptance remain required. Detailed scope, risks and reproducible tests: `Library/Balance_Sheet_Funding_Navigation_Trial_2026-09-21.md`.
