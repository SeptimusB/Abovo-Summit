# Developer Context Blueprint — Abovo Summit

Handover updated: 25 September 2026. Working checkout: `C:\Repos\Abovo Summit`; branch `main`. **Preserve tracked edits AND local untracked attachments.** Publish code/tests/technical notes only on `codex/checkpoint-2.98-code-only`; never push main's Word-containing ancestry. Verify current HEAD, publication branch and status rather than relying on an older checkpoint hash. Production test build remains **2.98** in `bin/Debug` and `bin/Release`. Excel-first implementation is staged and not enabled in production; Gear/Spread research remains separate. Current continuation: `Library/Excel_DevExpress_Engine_Stage2d_2026-09-25.md`.

## 1. System Architecture & Tech Stack

- **Product:** VB.NET WinForms, .NET Framework 4.8, DevExpress Ultimate 25.2.4: native Spreadsheet/Office API, Grid/VGrid/TreeList, charts, docking, editors; WebView2/HTML for selected summaries. Summit is an interface over an authoritative Excel workbook, not an independent financial database.
- **Workbook contract:** XLSB ordinarily; private XLSM trials/recovery supported. Preserve formulas, defined names, VBA, 3-D references, dynamic/legacy arrays, fills, locks and Excel → Summit → Excel/VBA round trips. Current masters: `Library/Blank BP v26_0001.xlsb` and `Library/Demo BP v26_0001.xlsb`; Debug auto-loads Demo. `TestFileClean.xlsb` is a historical contract baseline.
- **Definition/binding:** `Structure.xml` defines DITs, mapped tables, editors, `IsCalculated`, visibility/navigation, structural rules. `ExcelModels` holds per-model workbooks/state; `Services/FileManager.vb`, `Services/EngineManagement.vb`, `Services/DataService/ChangeManagerV2.vb` coordinate lifecycle, calculation and edits. `ModelChangeManager.ProcessChange` is the typed single-cell write/history/dirty/calculate/rollback path. `WorkbookStructureRuleManager`, structure services and `TransactionalDBSynchroniser` own multi-cell/structural operations.
- **State is not one dirty bit:** user/input revision, current calculation revision and accepted Check Sheet revision differ. Save/rebuild/preflight/result flags have different meanings. Save N cannot clear dirty N+1. Check Sheet imbalance is a soft accounting warning, distinct from invalid formulas/ranges/corruption; Yes/No overrides must remain respected and displayed as Yes/No. Discarded unsaved findings must not contaminate the saved file.
- **Calculation:** ordinary interaction uses `WBCalcEngine.CalculateWSs`; dependency-sensitive paths may temporarily use recursive workbook calculation and restore the prior engine in `Finally`. A sheet calculation is not proof of cross-sheet freshness. Native PMCost/RespCost implementations are registered explicitly; preserve PMCost's eight-position VBA signature, including unused compatibility argument.
- **Research only:** `Tools/SpreadsheetEngineTrial` is C# 7.3/.NET 4.8, x64, Gear **9.3.85**, DevExpress **25.2.4**, optional installed Excel COM and prior Aspose **26.9.0**. Nothing here is a production engine adapter. One serial owner/process per native workbook; Gear and native worker never share workbook objects. Gear licence is DPAPI/current-user outside the repository; never print or embed it.

## 2. The Core Objective

- **Production:** stabilize client-facing calculation/Check Sheet coherence and editor/zoom behavior without weakening workbook-owned editability or changing the financial model. Maintain an explicit Jon functional-test → Alex client-test → Alex agreement workflow.
- **Current requested work:** approved staged **Excel-authoritative workbook/calculation engine + configurable DevExpress fallback**, automatically preferring compatible installed Excel under existing macro policy. The read-only session foundation is implemented and tested separately; live display/edit/save ownership remains unimplemented. Retain the Excel-free contract. The Gear projection work below is historical research, not the active deployment plan. Never change masters or convert clients implicitly.
- **Two workloads:** build/update has structural/bulk edits; review has small edits whose implications must appear quickly. Gear may accelerate review; structural commands and complete serialization remain native. Native worker saves a private candidate at an exact acknowledged revision, verifies it, then replaces only a private trial target. Rebase Gear at a structural boundary.
- **Critical distinction:** skipping Gear drawing import solves an import obstacle, not feature compatibility. Gear must not save the reduced projection. Value-only edits can grow dynamic spills; native result fallback is required for unproven dependencies, not just structural changes. No certified automatic Gear eligibility map exists yet.
- User authorized isolated Excel/VBA benchmarks, including unprotecting disposable workbooks and the embedded Funding macros. That does not authorize force-enabling macros, Trust Center edits, attaching to existing Excel instances, customer-file overwrites or production integration.

## 3. Current Code State

**Production 2.98 — Ready to test, not client-approved:**

- `Services/CheckSheetWatch.vb`: guarded full calculation/read for a visible changed Check Sheet. `FileManager.vb`: accepted revision and `TryRecordCheckSheetResult`; distinct `CheckSheetValuesRefreshed` event, stale/lifecycle rejection, subscriber isolation. `ChangeManagerV2.vb`: write/calculate guard; refresh after accepted history changes. `EngineManagement.vb` and `DataInterfaceTemplate.vb`: model-specific visibility/warm-activation queue. `ReadOnlyMappedTableGrid.vb`: targeted cached value/color refresh preserving selection/scroll/width; pending Yes/No editor deferral and disposal guards.
- Evidence: `Library/Check_Sheet_Coherence_2.98_2026-09-24.md`; native candidate Debug/Release each passed **124/124 assertions**, including Funding break → Undo → Redo and mapped-grid/header coherence. UI/client tests remain required. No general full calculation was added to every ordinary edit.
- Checkpoint `f6b0ec8` includes the prior accumulated `GridPresentation.vb`, funding scheduler/view partials, `FundingSchedule*.vb`, editor/scale managers, snapshots, recovery/idle-integrity changes, `Structure.xml`, project compile includes and native fixtures. Local untracked Word/support attachments are deliberately excluded from publication, not disposable files.
- Scheduler: per-facility/section schedules; monthly/quarterly/semiannual/annual, end date or occurrence count, contiguous first-fit block with expansion plus five spare rows, optional fixed figures, Summit-only XML tints. Must not change Excel fills used for locking. Grid shortcuts are **Ctrl+wheel zoom / Shift+wheel horizontal / plain wheel vertical**. Model Version is read-only. Snapshot provisioning is explicit Create Snapshot, immediately after Transactional DB, not a blanket load migration.
- Ledger: `Library/Functional_Test_Ledger_2.97_2026-09-24.md` has a **2.98 update** despite its filename. IDs **1/2/3/5 → Send to Alex Test Response 3**; **4 failed mapped-table zoom/hscroll**; **10/60/80 → Ready to test — Jon, 2.98**. Current illustrated Word is `Library/Client_Review_2.93_v05.docx`; not updated by this engine-research turn.

**Excel-first foundation — new active source map:**

- Current checkpoint 2d: `Library/Excel_DevExpress_Engine_Stage2d_2026-09-25.md`. Default-off `EnablePublicationTrial` permits terminal `PublishAndCloseAsync` after verified candidates. Same-format new-name Save As or source-only expected-hash replacement; closes native owner, denies overlapping edits, retains backup plus intent/completion records, uses handle-based no-overwrite renames. **Brief missing-path window: not an atomic swap.** No live dirty/history acknowledgement, restart journal recovery or reopen/rebase yet. Captured Windows/MBAM provenance bytes and source discretionary ACL are preserved; unknown streams/storage profiles refuse. Test outputs use `bin/EngineStage2d-Debug` / `bin/EngineStage2d-Release`; normal 2.98 remains untouched.
- Checkpoint 2c: `Library/Excel_DevExpress_Engine_Stage2c_2026-09-25.md`. `EnableCandidateSaveTrial` (default false) enables revision-bound private candidates, source security-marker retention, XML/VBA preservation, native cached read-back and receipt/hash checks. Candidate creation alone cannot replace a file or acknowledge dirty state. AGL XLSB/XLSM candidates agree after full calculation in the other engine across 45,586 positions. Preservation is value-only: do not transplant baseline XML/VBA after structure or history changes. Native export/cleanup errors quarantine.
- Checkpoint 2b: `Library/Excel_DevExpress_Engine_Stage2b_2026-09-25.md`. `EnableValueEditTrial` (default false) enables guarded, typed in-memory edits, full calculation/read-back and compensated rollback through the same STA. No live history/dirty integration or engine switch. AGL opening-balance edit and restore agree across 45,586 positions in XLSB Debug/XLSM Release; original bytes unchanged. Single edit smoke trials range around 0.55-0.67 seconds Excel versus 6.5-9.4 seconds DevExpress, excluding UI refresh, not controlled benchmark medians. The existing change manager must still own interactive validation/history when connected.
- Checkpoint 2a: `Library/Excel_DevExpress_Engine_Stage2a_2026-09-25.md`. `WorkbookResultGridSource` binds detached, current engine results to native grids without changing formulas/fills. Scalar calculation interception is rejected for general projection because dynamic spill values become stale/missing. Open/calculate/close waits are bounded with quarantined late cleanup; process-level recovery is still pending. No live ownership switch or client release yet.
- Stage 3 must compare **all three** explicitly approved routes: existing DevExpress .NET commands, VB.NET driving Excel, and verified workbook VBA. Use `Library/Structural_Engine_Comparison_Protocol_2026-09-25.md`, full business-command equivalence and separate stage/total timings. Attachments and workbook fixtures stay local.

- `Services/WorkbookEngines`: immutable typed result boundary, one serialized STA owner, readonly baseline/hash/revision checks, Automatic/DevExpressOnly/ExcelRequired selection, opening-only fallback, existing-policy VBA and actual dynamic-array/UDF capability probes. Native failure faults the session; no silent mid-session swap. No live UI/edit/save integration yet.
- `Tools/WorkbookEngineFoundationTests`: deterministic safety, security screening, real x86 Excel/DevExpress comparison and cleanup. Current trial builds use `bin/EngineStage2d-Debug` and `bin/EngineStage2d-Release`, not normal outputs. See the checkpoint-2d report for current evidence and next gates, notably publication recovery/rebase, hung-operation supervision and formula/fill-preserving UI integration.

**Isolated Gear prototype — retained research source map:**

- `EngineBenchmark.cs`: native adapters, UDF registration, typed rectangle transport, owned Excel process. `MirrorTrial.cs`: durable revision/hash-chain journal, expected-before-value edits, worker acknowledgements, duplicate/gap rejection, save barrier, stale/locked-target rejection, private atomic replacement, restart/replay. `FundingTrial.cs`: actual Excel/VBA Funding comparator and prior raw-Gear experiments.
- `GearImportProbe.cs`: calculation projection uses `ReadObjects=false`, `ReadVBA=true`, manual foreground calculation. `RequireFullWorkbookForSave` prevents reduced Gear serialization before creating a file. All mirror initial/reopen/rebase Gear loads use this policy. No speculative Gear structural insert occurs while the native worker runs.
- `MirrorArrayCopy.cs`: research-only source-aware single-row dynamic-array copy; translated native destination formulas, multi-cell template refusal, native save/reopen and Gear synthetic checks. Full AGL verdict belongs in the current trial report, not inferred from the synthetic pass.
- **Third checkpoint:** `MirrorArrayTrace.cs` identifies stale DX dynamic-array registrations after row insert. `MirrorDynamicArrayBatch.cs` is now selected by the isolated DevExpress Funding worker: retain expressions, map whole-row shifts/template anchors, restore single-cell dynamic semantics before calculation. Multi-cell spills are refused. `MirrorArrayDiagnosis.cs` validates expected TDB array declarations in saved candidates. `MirrorArrayInsertProbe.cs` reproduces the defect with both Rows.Insert and bounded InsertCells and tests the guarded native/Gear reloads and export rejection. No production integration.
- `MirrorValues.cs`: explicit bounded native result request/full calculation; accept only matching session, immutable baseline hash, worksheet/rectangle and current journal revision. Late results cannot replace newer edits; values are displayed snapshots, never written over workbook formulas. Failed native calculation invalidates worker publication. This is a transport test, not integrated UI fallback or dependency classification.
- `Audit-MirrorTrial.py` and `Audit-FundingPackages.py`: independent probe/array/name/package/VBA audit. VBA source stays in memory; only hashes/module identities/attribute names leave it. External temporary `oletools` dependency, no raw VBA/passwords in repo. `compare-formulas` checks full formula text independently of cached values.
- Evidence root: ignored `obj/AsposeTrial/mirror-20260924-v2`; every run uses a fresh directory. See `Library/Spreadsheet_Mirror_Projection_Trial_2026-09-24.md` for final measurements, failed experiments and current gates. Older context: `Spreadsheet_Two_Engine_Prototype_2026-09-24.md`, `SpreadsheetGear_DevExpress_Import_Diagnosis_2026-09-24.md` and `SpreadsheetGear_Funding_Full_Trial_2026-09-24.md`.
- **Current continuation:** `Library/DevExpress_Array_Insert_Diagnosis_2026-09-24.md`, `obj/AsposeTrial/array-trace-20260924-batch-1` and `obj/AsposeTrial/mirror-20260924-v3-dx-agl`. Both independent audits match all 38,108 probes and array geometry; 335 VBA source bodies match. Native/Excel normal reopen passes within the report's limits. Full worker barrier is **302.73 s** (121.02 s array handling), review edits 0.55–0.76 s, save 15.48 s. Final Debug/Release synthetic safety and optional Excel synthetic tests pass. Next: optimize the costly guard/vendor reproduction, broaden structural and input-dependent spill coverage; do not deploy to production.

**Build/reproduce:** Visual Studio 2022 Enterprise MSBuild at `C:\Program Files\Microsoft Visual Studio\2022\Enterprise\MSBuild\Current\Bin\MSBuild.exe`; harness project `Tools/SpreadsheetEngineTrial/SpreadsheetEngineTrial.csproj`, `/p:PlatformTarget=x64 /p:Configuration=Debug` and `Release`. Executables: `Tools/SpreadsheetEngineTrial/bin/x64/{configuration}/net48/SpreadsheetEngineTrial.exe`. Native UDF/3-D helper references come from existing production `bin/Release`. Do not overwrite a running executable or change output nesting casually. Commands:

```text
mirror devexpress|excel model|synthetic PRIVATE_XLSM NEW_PRIVATE_DIRECTORY
mirror-array-copy NEW_PRIVATE_DIRECTORY
gear-projection PRIVATE_XLSM NEW_REPORT
compare-formulas PRIVATE_ORACLE PRIVATE_CANDIDATE NEW_REPORT
```

`model` is the reviewed AGL Funding fixture only, not arbitrary workbooks. Immutable common input: `obj/AsposeTrial/mirror-20260924-v1/dx-agl-2/baseline.xlsm`, SHA256 `62B44DE2764C4D1D1C9C7029C9297EEF4D461E300BAB4AD7DB092BBE1D23193C`. Independent Excel oracle: `obj/AsposeTrial/funding-d8bcacf09cdf4b308742ce2d4bf6645d/excel-1.json` and `.xlsm`. Create the explicit private parent first; do not weaken reparse-point/path checks.

## 4. Active Bugs & Blockers

- **Gear vendor-confirmed:** no grouped-sheet 3-D fix-up, no Custom XML preservation, no dynamic-array support (converted to fixed legacy arrays). Raw Gear Funding produces 12,050 wrong 3-D formulas; subsequent Excel calculation does not repair their references. AutoFill does not solve this. Do not serialize customer workbooks with Gear.
- **DevExpress → Gear:** object/drawing import triggers Gear's generic corrupt-OpenXML exception; skipping objects opens the same checkpoint. Exact offending drawing XML is not isolated. Chart sheets are not covered by `ReadObjects=false`. Native owner retains full package.
- **Both v2 DevExpress Funding attempts:** structural trials complete, but 3,729 TDB array declarations disappear although formula text remains; Gear's three TDB checks fail. Full formula addresses match 1,126,327 cells. Five defined-name text differences are equivalent single-cell rectangle normalization (`$A$8:$A$8` → `$A$8`), not demonstrated changed geometry. 570 formula spelling differences are RespCost casing samples; do not equate text inequality to financial errors. The source-aware row-copy retest did not fix the loss; the next investigation is staged native manifests through subsequent operations/calculation/serialization. Native reload also recognizes zero of the missing declarations as dynamic arrays. See the v2 report.
- **v3 supersedes that blocker for reviewed AGL Funding:** guarded output retains all 56,458 TDB arrays and the three failures clear. The raw API path remains defective. AGL XLSB reads as 51,948 legacy / 0 dynamic TDB arrays, while the converted XLSM reads as dynamic; this distinction prevents inferring a production XLSB regression from the prototype alone. The five-minute worker reply limit has little headroom around the 279.94-second structural command; performance and a production long-operation policy are still blockers.
- **Native fallback boundary:** initial synthetic seven-row spill collided with an existing merged explanatory row; DevExpress threw `KeyNotFoundException` during full calculation. Kept as evidence, not fixed or represented as a general Gear problem. Unobstructed 3→5→6-row test is separate. Unknown/error states must not publish stale values or be silently saved as verified.
- **Production pending:** mapped table zoom/horizontal scroll (ID4); Jon tests for 2.98 Check Sheet; Alex-wide double-click header-copy complaint remains unreproduced; client/DPI/funding/editor/clipboard/structural/financial acceptance remains on the ledger. Do not broaden this research turn into those fixes.
- **Not yet implemented/proven:** production engine adapter/ownership, full-model formula/function parity, Gear eligibility/dependency classification, dynamic spill changes across inputs, Development/OFA/Repairs add-delete mirror trials, evolving custom XML/history replay, comprehensive forms/VBA/UI round-trip, cancellation/timeouts/COM busy retry, crash/save recovery under real storage failures, minimum-spec performance. The trial restores original XML only because it has no XML-mutation commands; production must serialize current XML.

## 5. Code Style & Rules

- Read `AGENTS.md`; before workbook/engine/TDB/structural work read `Library/Abovo_Summit_Project_Scope_Audit.md`, `Abovo_Summit_Project_Index.json`, `Contract_XLSB_Audit_2026-08-24.md` and its evidence JSON. Historical facts are leads; verify active files/builds.
- Prefer supported native DevExpress controls/APIs. Preserve existing language/style; avoid unrelated refactors. Use `apply_patch` for edits, `rg` for discovery. No unrequested commits, resets, source-master writes, package changes, migrations or production engine switches.
- Interactive single-cell edits go through `ModelChangeManager`; structural writes require transaction/protection/dirty/rollback semantics. Respect workbook `Protection.Locked` and existing **fill/pattern-based editability**. User specifically requires notification before touching this logic. Read every displayed cell's workbook-owned formatting; do not infer new colors or editability from UI tints.
- Restore entry protection/calculation modes in `Finally`; never use VBA ABVCalculate's accidental early exit. Snapshot sheets are an explicitly authorized creation path, not permission for unrelated hidden sheets. All worksheet grids need selection/copy, including read-only ones.
- Excel inspection: private copies or read-only, events/macros disabled unless the exact isolated VBA operation is authorized; own and close only the process created for the test. Never force-enable macros, change Trust Center, touch existing Excel processes, expose credentials or export VBA source.
- Production test deliveries increment `AbovoApp` test version and build Debug/Release. Say **Ready to test**, not client acceptance. Jon tests first (blue); explicit handoff to Alex is orange heading/light-blue detail; green/Agreed only after Alex returns green text. Preserve stable test IDs. Timings from this high-spec workstation are not client guarantees.
- Use checkpoints, immutable originals and fresh disposable outputs. Keep durable compact evidence here, large/private workbooks only in ignored trial storage. Main originals' hashes and production executable hashes must be rechecked before handoff. A successful build/probe is not financial or full Excel/VBA certification.
