Namespace Abovo
    Public Class PrefsManager

        'Public Shared Function GetPrefs() As Dictionary(Of String, String)

        '    Dim Prefs As New Dictionary(Of String, String)
        '    Dim PrefsFile As String = Application.StartupPath & "\prefs.ini"

        '    If File.Exists(PrefsFile) Then

        '        Dim PrefsLines As String() = File.ReadAllLines(PrefsFile)

        '        For Each Line As String In PrefsLines

        '            Dim LineParts As String() = Line.Split("="c)

        '            If LineParts.Length = 2 Then

        '                Prefs.Add(LineParts(0), LineParts(1))

        '            End If

        '        Next

        '    End If

        '    Return Prefs

        'End Function

    End Class

End Namespace